# Todas las dependencias del proyecto: pipeline (scripts 01 a 08) + backend
# web (FastAPI). Versiones fijadas (==) para reproducibilidad exacta del TFM.
#
# Un único comando de instalación:
#   pip install -r requirements.txt
#
# (Hasta la versión anterior de este proyecto también existía una app web
# en Streamlit, que exigía protobuf>=5.26.1 —incompatible con el
# protobuf<5 de TensorFlow— y obligaba a instalar en 3 pasos separados con
# un pin manual de protobuf. Se eliminó esa app por quedar redundante frente
# a la versión FastAPI, así que ese conflicto ya no existe: todo se instala
# en una sola invocación, verificado de extremo a extremo en un venv nuevo.)

# Pipeline (EDA, preprocesamiento, entrenamiento, evaluación, Grad-CAM)
tensorflow==2.17.1
numpy==1.26.4
pandas==3.0.5
matplotlib==3.11.2
seaborn==0.13.2
scikit-learn==1.9.1
scikit-image==0.26.0
Pillow==12.3.0
opencv-python==4.11.0.86
joblib==1.6.0

# App web (backend FastAPI + frontend HTML/CSS/JS en src/frontend/)
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
