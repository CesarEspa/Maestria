# Dependencias del PIPELINE (scripts 01 a 08). Versiones fijadas (==) para
# reproducibilidad exacta del TFM.
#
# IMPORTANTE — instalar en este orden exacto (no combinar en un solo
# comando con requirements-web.txt): tensorflow exige protobuf<5 y
# streamlit exige protobuf>=5.26.1; son metadatos mutuamente excluyentes,
# así que pip solo puede resolverlos en INVOCACIONES SEPARADAS (dentro de
# una misma invocación, con ambos paquetes a la vez, pip falla con
# "ResolutionImpossible" — es una limitación real de pip, no un error de
# este archivo). En la práctica, protobuf 4.25.9 funciona bien en tiempo
# de ejecución con las dos librerías pese al conflicto declarado.
#
#   pip install -r requirements.txt
#   pip install -r requirements-web.txt
#   pip install "protobuf==4.25.9"
#
# (ver también la sección "Instalación" del README.md)
tensorflow==2.17.1
numpy==1.26.4
pandas==3.0.5
matplotlib==3.11.2
seaborn==0.13.2
scikit-learn==1.9.1
scikit-image==0.26.0
Pillow==12.3.0
opencv-python==4.11.0.86
joblib==1.6.0
