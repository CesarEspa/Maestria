====================================================================
PROYECTO CLAUDE - CONTEXTO COMPLETO
TFM: Detección y Clasificación Automática de Cáncer de Pulmón en
Imágenes de TC mediante Redes Neuronales Convolucionales y Aumento
de Datos
====================================================================

Para quién es este archivo:
  1. Para Claude (o cualquier IA) que retome este proyecto en una
     sesión nueva sin haber visto nada de lo anterior.
  2. Para César (el autor) como resumen propio de todo lo hecho.
  3. Como material base para redactar el documento TFM: metodología,
     resultados, discusión, limitaciones y conclusiones.

No es el README técnico (ese explica cómo instalar y ejecutar el
código). Este archivo explica QUÉ se hizo, QUÉ se encontró, y QUÉ
significa, con el detalle necesario para escribir el documento.

>>> PARA EMPEZAR A ESCRIBIR EL TFM DIRECTAMENTE: ir a la SECCIÓN 9
    ("Guion redactado, listo para adaptar al documento del TFM"), al
    final de este archivo. Está en el registro formal de una memoria
    de máster (introducción, objetivos con su cumplimiento, desarrollo,
    resultados, explicación de cada figura, discusión y conclusiones),
    lista para copiar y adaptar. Las secciones 1-8 son el material de
    referencia y el registro cronológico detrás de esos párrafos.


====================================================================
1. QUIÉN Y PARA QUÉ
====================================================================

Autor: César Libardo España Salguero, Ingeniero en Sistemas.
Programa: Máster Oficial en Inteligencia Artificial (Università G.
Marconi / AICAD Business School).

Título del TFM:
"Detección y Clasificación Automática de Cáncer de Pulmón en
Imágenes de Tomografía Computarizada mediante Redes Neuronales
Convolucionales y Aumento de Datos"

Objetivo general:
Desarrollar y evaluar un sistema de clasificación automática de
imágenes de TC de tórax en las categorías Normal, Benigno y Maligno,
basado en CNNs y técnicas de aumento de datos, orientado al apoyo
del proceso de cribado en servicios de radiología.

Objetivos específicos:
  - Preprocesar y analizar el dataset IQ-OTH/NCCD (normalización,
    partición estratificada 70/15/15, data augmentation).
  - Diseñar, entrenar y comparar 3 arquitecturas: CNN base, CNN con
    augmentation, y transfer learning (EfficientNetB0).
  - Evaluar con sensibilidad, especificidad, exactitud, AUC-ROC y
    matriz de confusión; contrastar con línea base SVM (HOG).
  - Generar Grad-CAM sobre el mejor modelo y analizar su
    aplicabilidad en un flujo de radiología.

Encuadre importante: el sistema se presenta como herramienta de
APOYO al diagnóstico (CADe/CADx), nunca como sustituto del médico.
Esto debe quedar explícito en la introducción y conclusiones del
documento TFM.


====================================================================
2. EL DATASET
====================================================================

IQ-OTH/NCCD Lung Cancer Dataset (Kaggle), TC de tórax recopiladas
por el Hospital Especializado de Oncología de Irak (IOSH) y el
Centro Nacional de Cáncer y Enfermedades del Tórax (NCCD).

  Clase                  N imágenes    %
  Benigno (Bengin cases)      120     10.9%
  Maligno (Malignant cases)   561     51.1%
  Normal (Normal cases)       416     37.9%
  TOTAL                      1097    100%

Carpetas reales en disco (nombres tal cual vienen del dataset,
incluida la errata "Bengin"):
  Datos/The IQ-OTHNCCD lung cancer dataset/Bengin cases/
  Datos/The IQ-OTHNCCD lung cancer dataset/Malignant cases/
  Datos/The IQ-OTHNCCD lung cancer dataset/Normal cases/

Características:
  - Dataset claramente DESBALANCEADO (Benigno tiene menos de la
    cuarta parte de imágenes que Maligno). Se mitigó con
    class_weight="balanced" en todos los modelos de Keras.
  - Tamaños originales heterogéneos (ancho 506-801 px, alto
    331-512 px). Se normalizó todo a 224x224 px.
  - Nombres de archivo tipo "Malignant case (37).jpg": son solo un
    índice secuencial, NO un identificador de paciente.

LIMITACIÓN CRÍTICA DEL DATASET (leer con atención, es el punto
metodológico más importante de todo el proyecto):

Las imágenes exportadas en Kaggle NO incluyen identificador de
paciente ni metadatos DICOM. Es sabido que en datasets de TC un
mismo paciente aporta muchos cortes axiales consecutivos, muy
parecidos entre sí. Como no hay forma de agrupar por paciente con
los datos disponibles, la partición train/val/test de este proyecto
se hizo A NIVEL DE IMAGEN, no a nivel de paciente. Esto es una
fuente de "fuga de datos" (data leakage): es probable que cortes
casi idénticos del mismo paciente queden repartidos entre
entrenamiento y prueba, inflando artificialmente las métricas de
generalización. La evidencia empírica de esto está en la sección 5
(la SVM llega a 100% de exactitud, algo no creíble para un método
clásico en un problema médico real). Esto DEBE mencionarse de forma
explícita en la sección de limitaciones del TFM, y matizar cualquier
afirmación de "el modelo generaliza bien".


====================================================================
3. QUÉ SE CONSTRUYÓ, EN ORDEN CRONOLÓGICO
====================================================================

FASE 1 — Pipeline de scripts (src/01_eda.py a src/08_baseline_ml.py)
----------------------------------------------------------------------
  01_eda.py            Análisis exploratorio: distribución de clases,
                        muestras por clase, distribución de tamaños,
                        distribución de intensidad de píxeles.
  02_preprocessing.py   Carga las 1097 imágenes, resize 224x224
                        (LANCZOS), normaliza a [0,1], y hace el split
                        estratificado 70/15/15 (semilla fija SEED=42).
                        Guarda todo en outputs/splits/dataset_splits.npz.
  03_train_cnn_base.py  CNN de 3 bloques Conv2D (32->64->128) +
                        BatchNorm + MaxPooling + GAP + Dense(256), SIN
                        aumento de datos.
  04_train_cnn_aug.py   Misma arquitectura, CON aumento de datos
                        (flip, rotación, zoom, contraste, traslación)
                        integrado como capa dentro del modelo.
  05_train_transfer.py  Transfer learning con EfficientNetB0
                        preentrenada en ImageNet. Fase 1: base
                        congelada, solo se entrena la cabeza (hasta 15
                        épocas). Fase 2: fine-tuning de las últimas 30
                        capas de EfficientNet (hasta 30 épocas más).
  06_evaluate.py         Evalúa los 3 modelos Keras sobre el conjunto
                        de TEST (165 imágenes): exactitud, AUC-ROC,
                        F1, sensibilidad por clase, matrices de
                        confusión, tabla comparativa.
  07_gradcam.py          Genera mapas de activación Grad-CAM sobre el
                        mejor modelo disponible (prioriza transfer
                        learning > CNN aug > CNN base).
  08_baseline_ml.py      Línea base de ML clásico: extrae
                        características HOG de cada imagen y entrena
                        un SVM (kernel RBF) como punto de comparación
                        frente a deep learning.

Todo el pipeline se ejecutó de principio a fin sobre CPU (sin GPU
disponible en el equipo de desarrollo). Tiempos aproximados: CNN base
y CNN+aug, 10-30 min cada una; transfer learning, ~6-10 min (fue
sorprendentemente rápido); SVM+HOG, 1-2 min.

FASE 2 — Primera app web: Streamlit (src/app.py)
----------------------------------------------------------------------
Para no depender solo de la terminal. Permite:
  - Lanzar el entrenamiento de cualquiera de los 4 modelos con un
    clic, y ver progreso en vivo (época actual, curvas de
    loss/accuracy, fase de entrenamiento).
  - Subir una imagen de TC o elegir una de ejemplo del dataset, y
    clasificarla con el modelo que se elija, viendo probabilidades
    por clase y el mapa Grad-CAM.
  - Ver la tabla comparativa de métricas de los 4 modelos.

Para esto se construyeron módulos reutilizables:
  - progress_tracker.py: un Callback de Keras que escribe el
    progreso del entrenamiento (época, loss, accuracy) a un JSON en
    outputs/progress/<modelo>.json, que la app va leyendo.
  - model_utils.py: preprocesa imágenes igual que 02_preprocessing.py
    y ejecuta inferencia con cualquiera de los 4 modelos.
  - gradcam_utils.py: genera el mapa de calor Grad-CAM y lo
    superpone sobre la imagen original.

FASE 3 — Segunda app web: frontend propio + backend FastAPI
(src/api.py + src/frontend/)
----------------------------------------------------------------------
Misma funcionalidad que la app de Streamlit, pero como una interfaz
HTML/CSS/JS propia (sin frameworks, sin build step) que habla con
una API REST hecha en FastAPI, en vez de todo-en-uno como Streamlit.
Se decidió así para tener una base más fácil de extender en el
futuro (autenticación, base de datos con historial de
clasificaciones, despliegue en un servidor real, etc.) sin tener que
reescribir la lógica de negocio.

Para evitar que las dos apps divergieran o mostraran números
distintos, se extrajo la lógica compartida a dos módulos nuevos:
  - training_control.py: lista de los 4 modelos entrenables, cómo
    lanzarlos/detenerlos como subprocesos, y cómo determinar su
    estado actual (idle/running/completed/failed/stale).
  - results_utils.py: carga de métricas y figuras de evaluación.

La API expone, entre otros: GET /api/models (estado de los 4
modelos), POST /api/models/{key}/train y /stop, GET /api/samples,
POST /api/classify (subir imagen) y /api/classify_sample (ejemplo
del dataset), GET /api/results, GET /api/figures y /api/gradcam_grid.
El backend sirve también el frontend estático, todo en un único
puerto (sin problemas de CORS).

FASE 4 — Iteración sobre la pestaña "Clasificar imagen"
----------------------------------------------------------------------
Se probó primero añadir una tercera pestaña con una rejilla de
imágenes tomadas directamente del conjunto de TEST (165 imágenes),
para poder clasificar imágenes que el modelo garantizadamente no
vio en entrenamiento. El usuario (César) no quiso esa rejilla
visualmente, así que se revirtió a las 2 pestañas originales ("Subir
mi imagen" / "Usar imagen de ejemplo") y se sustituyó por un botón
único, "Elegir imagen aleatoria", que toma una imagen al azar del
30% de datos NO usado en entrenamiento (validación + test
combinados, 330 imágenes en total) para comprobar rápidamente "si el
modelo está bien entrenado" sobre datos nunca vistos.

FASE 5 — Segunda ronda de entrenamiento: intento de arreglar el colapso
----------------------------------------------------------------------
Con el diagnóstico de la Fase 1 en mano (ver sección 4 original: las
CNN entrenadas desde cero colapsaban a predecir una sola clase), el
usuario pidió intentar resolverlo aplicando directamente la hipótesis
de causa raíz, usando GPU si estaba disponible.

Se comprobó que, aunque el equipo tiene una GPU NVIDIA física (RTX
3050), la instalación de TensorFlow no podía usarla: desde TF 2.11,
`pip install tensorflow` ya no trae soporte nativo de GPU en Windows.
Usarla de verdad habría requerido WSL2 o el plugin DirectML de
Microsoft (con riesgo de romper el código Keras 3 ya probado), así
que se decidió reentrenar en CPU en vez de arriesgar el entorno.

Cambios aplicados a CNN Base y CNN + Augmentation:
  - LayerNormalization en vez de BatchNormalization (no depende de
    estadísticas de batch, inmune al problema de pocos batches/época).
  - Learning rate bajado de 1e-3 a 1e-4.
  - Loss con label_smoothing=0.1 (requirió cambiar a
    CategoricalCrossentropy + etiquetas one-hot).
  - EarlyStopping: paciencia subida de 7 a 12; presupuesto de épocas
    subido de 50 a 80.
  - Solo en CNN + Augmentation: augmentation reducido (se quitó
    RandomFlip vertical y RandomTranslation; RandomRotation y
    RandomZoom con menor magnitud).

Cambios aplicados a Transfer Learning:
  - Fine-tuning de las últimas 50 capas de EfficientNetB0 (antes 30).
  - Presupuesto de épocas de la fase de fine-tuning subido de 30 a 50.

RESULTADO (ver sección 4 para el detalle completo, ya actualizada con
estos números): la CNN Base se arregló POR COMPLETO (50.9% -> 84.85%
de exactitud, ya no colapsa). La CNN + Augmentation solo mejoró
parcialmente: su AUC-ROC subió de 0.503 a 0.728, pero la exactitud
final (50.9%) sigue correspondiendo a un colapso hacia la clase
mayoritaria. La Transfer Learning mejoró de 70.9% a 83.0% de
exactitud y de 0.913 a 0.976 de AUC-ROC. El NO haber arreglado del
todo la CNN + Augmentation es, en sí mismo, un resultado informativo:
como la CNN Base (misma arquitectura e hiperparámetros, sin
augmentation) sí se arregló, la diferencia solo puede deberse al
augmentation en tiempo real — evidencia de que el augmentation es un
factor causal adicional de inestabilidad en este dataset pequeño, no
solo la normalización o el learning rate.

También se extendió 06_evaluate.py para generar curvas ROC
comparativas de los 4 modelos (macro-average, superpuestas en una
figura) y el classification_report de cada modelo como imagen, y se
regeneró el grid de Grad-CAM con el nuevo mejor modelo (el problema
de la esquina saturada, sección 5.3 original, persiste sin cambios).

FASE 6 — Mejoras de UX en el panel de Entrenamiento (solo app FastAPI)
----------------------------------------------------------------------
El usuario pidió tres mejoras concretas sobre la pestaña
"Entrenamiento" de la app FastAPI + frontend propio (src/api.py +
src/frontend/): que el panel "Ver log del proceso" dejara de cerrarse
solo cada vez que la página se refresca (cada 3 segundos mientras hay
algo entrenando), poder elegir el número de épocas antes de lanzar un
modelo (en vez del valor fijo de config.py), y un botón para lanzar
los 4 modelos de una sola vez.

Se implementaron los tres, solo en esta app (NO en la app Streamlit,
que sigue con el flujo original de un único botón "Entrenar" por
modelo — pendiente como trabajo futuro llevarle los mismos controles):
  - El estado de qué logs están desplegados ahora vive en el frontend
    (frontend/app.js, state.openLogs) y se reaplica en cada
    re-render, en vez de perderse; de paso se limpiaron los códigos
    de color ANSI que Keras escribe en el log (antes eran ruido
    visual tipo "[1m", "[32m", invisible mientras el panel se cerraba
    solo, pero evidente en cuanto se dejó desplegado).
  - El número de épocas elegido por el usuario se manda al backend
    como parámetro de query (?epochs=N) en POST /api/models/{key}/train,
    y los scripts 03/04/05 lo leen vía la variable de entorno
    TFM_EPOCHS_OVERRIDE (sin tocar config.py). En transfer learning
    solo afecta a la fase 2 (fine-tuning); la fase 1 se mantiene fija
    en 15 épocas.
  - "Entrenar todos los modelos" lanza los 4 en cola SECUENCIAL (uno
    detrás de otro, no en paralelo) porque el equipo solo tiene CPU y
    entrenar varios a la vez los haría competir por el mismo
    procesador. El frontend vigila el estado del modelo actual y
    lanza el siguiente en cuanto el anterior llega a un estado
    terminal (completado/fallido/detenido/sin actividad).

Mientras se probaban estos cambios contra la app real (no solo
revisión de código: se relanzó un entrenamiento real de CNN Base con
pocas épocas y se probó el ciclo completo lanzar->detener), se
encontró y corrigió un BUG REAL preexistente en el botón "Detener":
mataba el proceso correctamente, pero nunca actualizaba el archivo de
progreso para reflejarlo, así que la tarjeta se quedaba mostrando
"Entrenando..." con la barra congelada durante hasta 5 minutos (el
umbral de "sin actividad reciente"), dando la sensación de que la app
se había colgado aunque el proceso ya estaba muerto. Se corrigió en
training_control.py y api.py — ver el detalle técnico en la sección
5.5. También se encontró, como efecto colateral de las pruebas, que
reiniciar el proceso del servidor de desarrollo puede matar procesos
de entrenamiento hijos que siguieran corriendo en ese momento (no hay
forma de reiniciar el backend sin arriesgar interrumpir un
entrenamiento activo en este entorno) — algo a tener en cuenta en
próximas sesiones de mantenimiento.

FASE 7 — Incidente: reentrenamiento accidental de CNN Base y restauración
----------------------------------------------------------------------
El 23/09/2026, al revisar cuántas épocas había usado realmente cada
modelo, se detectó que outputs/models/cnn_base.keras y
outputs/figures/curvas_cnn_base.png habían sido sobrescritos por un
reentrenamiento reciente (mismo botón "Reentrenar" de la app, con el
presupuesto por defecto de 80 épocas) que volvió a colapsar: paró en
la época 14 con la accuracy de validación congelada en 10.9%, el
mismo patrón del Hallazgo/sección 5.1. Las métricas oficiales
(metricas_comparativas.json, generadas el día anterior) seguían
mostrando el 84.85% de exactitud documentado, así que hubo un breve
desajuste entre el archivo del modelo en disco y lo reportado en
README/este documento.

Se restauró el modelo bueno con "git checkout HEAD -- <archivo>",
porque outputs/models/ y outputs/figures/ SÍ están versionados en git
(solo outputs/progress/ está en .gitignore), y el commit 1efd2ac de la
segunda ronda de entrenamiento (Fase 5) ya contenía el .keras bueno.
Confirmado con md5sum que el archivo restaurado es byte a byte
idéntico al de ese commit. Durante la verificación posterior también
se lanzó por error, vía un comando mal escrito, OTRO reentrenamiento
de CNN Base; se detectó de inmediato (proceso vivo corriendo
03_train_cnn_base.py) y se mató con taskkill antes de que llegara a
guardar nada (el .save() del modelo solo ocurre al final de
model.fit(), así que no hubo ningún impacto real).

LECCIÓN para futuras sesiones: como outputs/models/ y outputs/figures/
están versionados en git, cualquier archivo de modelo o figura
sobrescrito por un reentrenamiento accidental (propio o del usuario)
es recuperable con "git status outputs/" (para ver qué cambió) seguido
de "git checkout HEAD -- <archivo>". Es buena práctica comprobarlo
antes de creer que un resultado documentado sigue siendo válido, sobre
todo después de que alguien haya usado el botón "Reentrenar"/"Entrenar
todos" de la app.

FASE 8 — Tercera ronda de entrenamiento: presupuesto de épocas más alto
----------------------------------------------------------------------
El usuario pidió: (1) mostrar en la UI el límite de épocas de cada
modelo junto al campo para elegirlas, (2) mostrar en algún lugar con
cuántas épocas quedó entrenado cada modelo, y (3) reentrenar los 4
modelos al máximo de épocas razonable, ajustando después las
conclusiones documentadas con los resultados reales.

Cambios de UI (frontend/app.js, frontend/style.css): la tarjeta de
cada modelo ahora muestra "límite actual: N" junto al campo de
épocas, y cuando el modelo está completado aparece un chip "Épocas
entrenadas" con el valor real (leído de current_epoch en el JSON de
progreso, que ya se registraba desde la Fase 5 — solo faltaba
mostrarlo en la interfaz).

Presupuestos subidos en config.py y 05_train_transfer.py:
  EPOCHS_BASE (CNN base/aug):        80  -> 120
  PHASE1_EPOCHS (transfer, fase 1):  15  -> 25
  EPOCHS_TRANSFER (transfer, fase 2): 50 -> 100
Motivo: en la ronda anterior, la fase 2 de transfer learning había
agotado su presupuesto completo (50/50) sin que EarlyStopping llegara
a activarse, señal de que probablemente seguía mejorando. Las CNN base
y aug ya paraban muy por debajo de su techo (38 y 14 de 80), así que
para ellas la subida era más una comprobación de seguridad que una
necesidad detectada.

Se relanzaron los 4 modelos en secuencia (03, 04, 05, 08) seguidos de
06_evaluate.py y 07_gradcam.py, todo en un único comando encadenado
con && para que se detuviera si algún paso fallaba. Duración real
total: ~85 minutos (17:04 a 18:29) — mucho menos de lo estimado
inicialmente (2-4 h), porque EarlyStopping siguió cortando temprano en
las CNN y la fase 2 de transfer learning solo llegó a 52 de las 100
épocas disponibles.

RESULTADO — desigual entre los tres modelos de Keras (comparado con la
segunda ronda, 80/50 épocas):

  CNN Base: MEJORÓ. 84.85% -> 86.67% exactitud, AUC 0.939 -> 0.945,
  F1 macro 0.730 -> 0.749. Paró en la época 34 (mejor: época 22, antes
  paraba en ~38 con techo de 80 pero sin registro exacto disponible —
  ver Fase 7). Confirma que este modelo SÍ estaba limitado por el
  techo anterior.

  CNN + Augmentation: NO MEJORÓ, y paró todavía antes (13 épocas en
  vez de 14; su "mejor" punto fue la propia época 1, nunca volvió a
  bajar el val_loss inicial). Exactitud IDÉNTICA (50.91%) porque el
  modelo colapsa exactamente igual (predice "Maligno" para casi todo
  el test). Esto descarta con más fuerza que el colapso sea un
  problema de presupuesto de épocas: con el triple de margen
  disponible, el modelo ni siquiera lo intenta más allá de la primera
  época.

  Transfer Learning: EMPEORÓ en test, pese a usar más épocas reales.
  83.03% -> 79.39% exactitud, AUC 0.976 -> 0.956, F1 macro 0.776 ->
  0.733, sensibilidad Benigno 0.89 -> 0.83. La fase 2 sí aprovechó el
  presupuesto extra (52 épocas en vez de 50) y las métricas de
  VALIDACIÓN del punto elegido por EarlyStopping fueron similares o
  mejores (val_accuracy 0.830, val_loss 0.499) — es decir, el punto
  que pareció mejor en validación no generalizó igual de bien a test.
  Interpretación: con test y validación de solo 165 imágenes cada uno,
  ambos son pequeños y ruidosos; esto es evidencia práctica (no solo
  teórica) de por qué la validación cruzada (k-fold) sería una mejora
  metodológica real — ver sección 7.

  SVM (HOG): se re-ejecutó por completo (era parte de "los 4
  modelos"), pero al ser un método determinista (sin conceptos de
  época) produjo un archivo .joblib byte a byte idéntico al anterior
  — mismo resultado sospechoso de fuga de datos, sin cambios.

Grad-CAM también se regeneró con el nuevo modelo de transfer learning
y el mismo patrón de saturación en una esquina (sección 5.3) persiste
sin cambios — ya son tres entrenamientos distintos de transfer
learning mostrando el mismo artefacto, lo que descarta que sea
casualidad de un entrenamiento concreto.

CONCLUSIÓN PARA EL TFM: subir el presupuesto de épocas no es una
mejora automática. Ayudó al modelo que realmente estaba limitado por
el techo (CNN Base), no cambió nada en el modelo cuyo problema es
estructural (CNN + Augmentation), y en Transfer Learning introdujo
varianza que empeoró la cifra de test pese a métricas de validación
similares. Los tres resultados, incluido el que "salió peor", se
documentan tal cual — es un hallazgo metodológico legítimo sobre
optimizar y evaluar con datasets pequeños.

FASE 9 — Rediseño visual, reordenar el campo de épocas, y eliminar la app Streamlit
----------------------------------------------------------------------
El usuario pidió tres cosas en esta fase, en mensajes separados: (1) un
diseño de frontend más minimalista y menos "genérico de IA", con las
notas de cada modelo en lenguaje entendible y los botones alineados
entre tarjetas; (2) mover el campo de épocas al lado izquierdo del
botón Reentrenar (en vez de en su propia fila, como había quedado tras
el primer intento de arreglar la alineación); y (3), tras preguntar
"¿crees que puedas optimizar el proyecto, todo lo que tiene es útil e
importante?", una auditoría de qué sobraba en el proyecto.

Rediseño (src/frontend/style.css, app.js): paleta más plana y
restringida (un único color de acento en vez de 5-6 compitiendo),
insignias de estado como punto de color + texto plano en vez de
píldoras con fondo, chips de métricas como fila de datos simple
(etiqueta pequeña + número grande) en vez de cajas con fondo, sin
ícono de marca con degradado ni sombras pesadas en las tarjetas. Las
notas de cada modelo (training_control.py, campo "note") se
reescribieron en lenguaje llano, sin jerga técnica (antes: "LayerNorm
+ LR 1e-4, hasta 120 épocas (EarlyStopping paciencia 12)"; ahora: "Red
neuronal entrenada desde cero, sin variar las imágenes. Tarda entre 35
y 50 minutos") — el detalle técnico exacto se mantiene en este
documento y en el README, no en la interfaz.

El problema real de alineación de botones era estructural: el campo de
épocas compartía fila con el botón, y como el SVM no tiene ese campo
su fila quedaba más corta, desalineando su botón respecto a los otros
3. Primero se resolvió moviendo el campo a su propia fila debajo del
título (quedaba simétrico, pero el usuario pidió después volver a
ponerlo junto al botón). Se resolvió definitivamente haciendo el campo
compacto de una sola línea (con el detalle de "máximo permitido" y
"modelo actual" en un tooltip en vez de texto visible siempre), para
que quepa a la izquierda del botón sin cambiar la altura de la fila.
Durante ese último cambio se introdujo y luego se corrigió un bug real
propio: quedó una referencia a una variable renombrada
(`epochRow` en vez de `epochField`), que rompía por completo el
renderizado de la página de Entrenamiento (las tarjetas no se
dibujaban); se detectó ejecutando la página con Playwright y leyendo
el error real de JavaScript en vez de asumir que "no restringía el
máximo" era el único problema.

Auditoría y eliminación de la app Streamlit: revisando qué tenía cada
interfaz (ver comparación detallada más abajo), se confirmó que
src/app.py (Streamlit) se había quedado sin las funciones añadidas en
las Fases 6, 8 y 9 a la app FastAPI (épocas configurables, entrenar
todos, log persistente, botón Detener corregido, rediseño), y que
además su propio botón Detener seguía sin el arreglo del bug de la
Fase 8 (llama a kill_process() pero nunca actualiza el estado a
"stopped", así que su UI se quedaría mostrando "Entrenando..." hasta 5
minutos tras detener algo). Con la aprobación explícita del usuario, se
eliminó src/app.py.

Al eliminar Streamlit se descubrió una simplificación adicional
importante, verificada con "pip show protobuf" en el venv existente:
"Required-by: streamlit, tensorboard, tensorflow-intel" — es decir,
Streamlit era el ÚNICO paquete que exigía protobuf>=5.26.1; ni
FastAPI ni uvicorn dependen de protobuf en absoluto. Se probó
instalando requirements.txt (pipeline) + fastapi/uvicorn/
python-multipart en un venv nuevo, EN UN SOLO COMANDO, y no hubo
ningún conflicto de dependencias — a diferencia de cuando streamlit
estaba en la mezcla. Se fusionaron requirements.txt y
requirements-web.txt en un único requirements.txt, se eliminó
requirements-web.txt, y la instalación pasó de 3 pasos con un pin
manual de protobuf a un solo "pip install -r requirements.txt".

También se limpió código muerto encontrado durante la comparación de
las dos apps: la clase TrainRequest en api.py (un BaseModel de
Pydantic vacío, sin uso real desde que los parámetros de entrenamiento
van por query string) y su import de BaseModel, y los comentarios/
docstrings en training_control.py, api.py, results_utils.py y
progress_tracker.py que todavía mencionaban la app Streamlit como si
siguiera existiendo.

COMPARACIÓN QUE MOTIVÓ LA DECISIÓN (para referencia futura): ambas
apps compartían la misma lógica de negocio (mismos números y
predicciones), pero Streamlit no tenía épocas configurables, ni
"Entrenar todos", ni el botón Detener corregido, ni el rediseño
visual; y su propio mecanismo de refresco (recargar toda la página
cada 3 s con st.rerun()) era más disruptivo que el de la app FastAPI
(que solo actualiza las partes que cambian).

FASE 10 — Alinear el proyecto con la Propuesta 1 oficial del TFM (Tarea 1: especificidad)
----------------------------------------------------------------------
El usuario comparó el proyecto con el documento oficial de propuestas
de temas del máster y detectó tres huecos frente a lo que promete la
"Propuesta 1": (a) el objetivo específico 3 pide explícitamente
comparar sensibilidad Y especificidad, y el proyecto solo reportaba
sensibilidad; (b) el objetivo específico 1 pide segmentación, que no
existía; (c) el resumen de la propuesta menciona "aumento de datos
avanzado, como el intercambio de píxeles" (CutMix/Mixup), y el
proyecto solo usa aumento estándar (flip/rotación/zoom/contraste).
Pidió cerrar los tres huecos en orden estricto de prioridad,
deteniéndose a mostrar resultados entre cada tarea. Esta fase cubre
solo la TAREA 1 (especificidad) — prioritaria porque el objetivo la
nombra literalmente y es la más rápida de resolver sin reentrenar nada.

Implementación (src/06_evaluate.py y src/08_baseline_ml.py):
  - specificity_per_class_from_cm(cm): especificidad uno-contra-el-resto
    por clase desde la matriz de confusión. Para la clase i:
    FP_i = suma de la columna i excluyendo la diagonal; VN_i = total -
    fila_i - columna_i + cm[i,i]; Especificidad_i = VN_i/(VN_i+FP_i).
    Verificado a mano contra CNN Base/Benigno: VN=135, FP=12,
    135/147=0.9184, coincide exacto con lo que calculó el script.
  - Se añadió specificity_per_class y specificity_macro a
    metricas_comparativas.json (los 3 modelos Keras) y, VÍA UN PARCHE
    DE SOLO LECTURA (sin volver a llamar a svm.fit()), a
    metricas_svm_baseline.json — 06_evaluate.py ya cargaba la SVM en
    modo lectura para las curvas ROC comparativas, así que se
    reutilizó esa misma carga para calcular su especificidad y
    reescribir el JSON solo con los 2 campos nuevos, sin tocar nada
    más del archivo ni reentrenar el modelo.
  - 08_baseline_ml.py también se actualizó con la misma función (para
    que un reentrenamiento LEGÍTIMO futuro de la SVM ya la incluya de
    fábrica), pero ese script NO SE EJECUTÓ en esta fase — se respetó
    al pie de la letra la instrucción de no reentrenar la SVM.
  - Nueva figura tabla_comparativa_especificidad.png: misma estética
    que tabla_comparativa.png (naranja en vez de azul, para
    diferenciarla a simple vista), con Especif. Benigno/Maligno/Normal
    y Especif. Macro, para los 3 modelos de Keras.
  - Nueva figura sensibilidad_especificidad.png: gráfico de barras
    agrupadas, 3 paneles (uno por clase), sensibilidad vs especificidad
    para los 4 modelos. Es la figura que expone con más claridad el
    colapso de CNN + Augmentation (ver más abajo).
  - tabla_comparativa.png y confusion_matrix_*.png se regeneraron sin
    cambios de contenido (mismo resultado, ya que 06_evaluate.py es
    puramente de inferencia — se confirmó con "git status
    outputs/models/" que ningún .keras/.joblib cambió un solo byte).

RESULTADO — especificidad macro por modelo: CNN Base 0.9401, CNN +
Augmentation 0.6667 (la más baja), Transfer Learning 0.9139, SVM 1.0000.
El hallazgo más informativo es el de CNN + Augmentation: especificidad
1.00 en Benigno y en Normal, pero 0.00 en Maligno. Como el modelo
predice "Maligno" para prácticamente todo el conjunto de test, nunca
tiene falsos positivos en las otras dos clases (nunca las predice, así
que su especificidad ahí es perfecta por una razón trivial, no por
mérito real) y, en cambio, jamás descarta correctamente a nadie de ser
Maligno (especificidad 0 = no sirve ni para "confirmar ausencia" de esa
clase). Esto refuerza con una métrica adicional, independiente de la
sensibilidad, la misma conclusión de la sección 4.2/Hallazgo 3: el
colapso es real y no es un artefacto de cómo se mide.

Tabla completa (sensibilidad y especificidad, los 4 modelos — la que
pidió el usuario ver antes de continuar a la Tarea 2):

Modelo                Exactitud  AUC-ROC  F1 Macro  Sens.Ben  Sens.Mal  Sens.Nor  Espec.Ben  Espec.Mal  Espec.Nor  Espec.Macro
CNN Base                0.8667    0.9449    0.7490    0.4444    0.9881    0.8254    0.9184     1.0000     0.9020     0.9401
CNN + Augmentation      0.5091    0.7233    0.2249    0.0000    1.0000    0.0000    1.0000     0.0000     1.0000     0.6667
Transfer Learning       0.7939    0.9563    0.7330    0.8333    0.8571    0.6984    0.8299     1.0000     0.9118     0.9139
SVM (HOG) línea base    1.0000    1.0000    1.0000    1.0000    1.0000    1.0000    1.0000     1.0000     1.0000     1.0000

Commit y push de esta tarea hechos por separado, antes de continuar a
la Tarea 2 (segmentación), tal como pidió el usuario.

TAREA 2 (Fase 10) — Segmentación pulmonar clásica
----------------------------------------------------------------------
El objetivo específico 1 de la propuesta oficial pide "implementar
técnicas de segmentación" en el preprocesamiento; el proyecto no la
tenía. Se implementó una segmentación clásica (sin deep learning: no
había tiempo ni datos anotados para entrenar una U-Net) y se
reentrenó SOLO Transfer Learning sobre esos datos, tal como se pidió.

ALGORITMO (src/02b_segmentation.py), en el orden en que realmente
funcionó (no en el orden en que se intentó por primera vez):
  1. Escala de grises + umbral de Otsu (no HU: son JPG exportados, no
     DICOM crudo, así que no se asumió ningún rango Hounsfield).
  2. Silueta corporal = el componente CLARO (brillante) más grande de
     la imagen + cierre morfológico + relleno de huecos → una región
     corporal sólida.
  3. Candidatos a pulmón = píxeles OSCUROS que caen DENTRO de esa
     silueta corporal (no solo "oscuros y no tocan el borde", que fue
     el primer intento — ver el bug real más abajo).
  4. Apertura + cierre + descartar componentes diminutos; quedarse con
     los 2 componentes conexos más grandes = los dos pulmones.
  5. Cierre final más agresivo (disco de radio 9) + relleno de huecos,
     para no amputar masas brillantes que tocan el borde del pulmón.

BUG REAL ENCONTRADO Y CORREGIDO DURANTE LA VALIDACIÓN (exactamente el
tipo de fallo que el usuario pidió vigilar explícitamente: "si la
segmentación... deja pasar la mesa del escáner, ajusta los
parámetros"): la primera versión del algoritmo (sin restringir a la
silueta corporal, solo "oscuro + no toca el borde + top-2 por área")
dejaba pasar la mesa del escáner en imágenes donde el corte pulmonar
es pequeño (cerca del ápice o la base del pulmón): el hueco oscuro
bajo el arco de la mesa no toca el borde de la imagen y a veces es
más grande que los propios pulmones en esas rebanadas, así que ganaba
la selección "top-2 por área". Se encontró revisando visualmente 6
ejemplos (imagen de train #1 fue el caso que falló: la máscara
incluía la curva completa de la mesa). Se corrigió restringiendo los
candidatos al interior de la silueta corporal (paso 2-3 arriba), lo
que excluye estructuralmente cualquier cosa fuera del cuerpo, sin
importar su tamaño. Verificado que el fix funciona en la MISMA imagen
que había fallado, más 9 ejemplos adicionales (3 por clase) y,
después, 150 imágenes aleatorias del set de entrenamiento con
verificación estadística automática (fracción de área de la máscara:
mínimo 3.5%, sin máscaras vacías; un único caso con área alta, 44%,
resultó ser una rebanada pulmonar genuinamente grande al inspeccionarla,
no un fallo). Solo después de esta doble validación (visual +
estadística) se procesó el dataset completo (1097 imágenes).

Se guardó como outputs/splits/dataset_splits_segmented.npz, DERIVADO
DIRECTAMENTE de dataset_splits.npz (mismos X ya cargados, solo se les
aplica segment_lungs() y se guardan con las MISMAS y_train/y_val/y_test)
— no se rehizo el split, así que las particiones y etiquetas son
IDÉNTICAS por construcción, verificado con np.array_equal en las 3
particiones (True en las 3).

REENTRENAMIENTO (src/05b_train_transfer_segmented.py): arquitectura e
hiperparámetros IDÉNTICOS a 05_train_transfer.py (mismo LR, mismas 50
capas descongeladas, mismo presupuesto de épocas 25+100), cambiando
solo la fuente de datos (dataset_splits_segmented.npz) y el nombre del
modelo guardado (transfer_efficientnet_segmented.keras, NO sobreescribe
el original). Entrenamiento estable, sin señales de colapso: val_loss
0.5914, val_accuracy 0.7636 al final.

EVALUACIÓN (06_evaluate.py extendido con un 5º modelo): se evaluó
sobre el TEST SEGMENTADO (no el original), porque el modelo espera esa
distribución de entrada — comparar contra el test sin segmentar no
sería válido. Resultado, comparado con Transfer Learning original:

Métrica              Original  Segmentado  Cambio
Exactitud             0.7939    0.7879     casi igual (-0.006)
AUC-ROC                0.9563    0.9118     BAJÓ
F1 Macro                0.7330    0.6968     BAJÓ
Sens. Benigno            0.8333    0.5556     BAJÓ MUCHO (la mejora estrella del original)
Sens. Maligno             0.8571    0.8929     subió
Sens. Normal               0.6984    0.7143     subió levemente
Espec. Benigno              0.8299    0.8571     subió
Espec. Maligno                1.0000    0.9630     bajó
Espec. Normal                  0.9118    0.8922     bajó

CONCLUSIÓN: la segmentación NO mejoró el modelo. La exactitud se
mantiene casi igual, pero el AUC-ROC y el F1 macro bajan, y sobre todo
se pierde gran parte de la ganancia en sensibilidad de Benigno que
era el resultado más valioso del modelo original (0.83 → 0.56).
Hipótesis: eliminar el fondo y la mesa del escáner también elimina
contexto que la red aprovechaba de forma implícita (posición/forma del
cuerpo, caja torácica), y la segmentación clásica —imperfecta— añade
su propio ruido (bordes irregulares, recorte parcial de masas grandes
que tocan el borde pulmonar, visible en ejemplos_segmentacion.png).

GRAD-CAM (src/07b_gradcam_segmentado.py) — LA PREGUNTA CONCRETA DEL
USUARIO: "¿desaparece el artefacto de la esquina saturada al eliminar
el fondo y la mesa del escáner?" Respuesta: el artefacto ORIGINAL
(un único píxel saturado, siempre en la esquina inferior derecha,
sección 5.3) SÍ desaparece — pero es reemplazado por uno DISTINTO y,
verificado cuantitativamente, TODAVÍA MÁS independiente del contenido
de la imagen: un degradado suave izquierda-derecha que ignora por
completo la forma de la máscara pulmonar (se extiende sobre el fondo
negro igual que sobre el pulmón). Verificación: se comparó la
correlación de Pearson entre los mapas de calor CRUDOS (7×7, antes de
superponer nada) de 3 imágenes de clases y contenido completamente
distintos → 0.999 de correlación. Es decir, el mapa es, en la
práctica, CONSTANTE sin importar la imagen de entrada — un problema
igual de grave que el original, solo que con una forma visual distinta
(antes un punto, ahora un degradado) que por eso mismo es más fácil de
pasar por alto con solo mirar las imágenes sin medir la correlación.

CONCLUSIÓN PARA EL TFM (ambos resultados, honestos, tal como se
pidió): ni la exactitud ni la explicabilidad mejoraron con este
experimento de segmentación clásica. Esto no invalida la segmentación
como idea — el objetivo específico 1 de la propuesta queda cumplido
porque SE IMPLEMENTÓ y se evaluó rigurosamente, con validación previa
documentada (incluido el bug real encontrado y corregido) — pero el
enfoque clásico concreto usado aquí no demostró beneficio frente al
pipeline sin segmentar, y el patrón de Grad-CAM "constante" persiste
bajo una forma distinta, reforzando que su causa es más probablemente
una propiedad estructural de EfficientNetB0 que algo relacionado con
el fondo de la imagen.

Commit y push de esta tarea hechos por separado, antes de continuar a
la Tarea 3 (CutMix/Mixup), a la espera de confirmación del usuario.

TAREA 3 (Fase 10) — CutMix (aumento de datos avanzado, "intercambio de
píxeles" citado en el resumen de la propuesta 1 del TFM)

Objetivo: implementar CutMix (Yun et al., 2019) en un script nuevo
(src/04b_train_cnn_cutmix.py), manteniendo EXACTAMENTE la misma
arquitectura, optimizador, loss, presupuesto de épocas, paciencia,
class_weight y semilla que 04_train_cnn_aug.py, para poder contrastar
limpiamente la hipótesis: ¿evita CutMix el colapso que causa el
augmentation estándar (exactitud 50.91%, predice "Maligno" para todo,
sección 4.2)?

DECISIÓN TÉCNICA CLAVE: CutMix no se puede implementar como una capa
de Keras dentro del grafo del modelo (como sí hace build_augmentation_layer()
para el augmentation estándar), porque necesita acceso a una SEGUNDA
imagen y su etiqueta —una capa de preprocesamiento no tiene acceso a
las etiquetas—. Se implementó como una función vectorizada
(cutmix_batch) aplicada sobre un tf.data.Dataset con
.batch().map(cutmix_batch), en vez del patrón anterior
model.fit(X_train, y_train_cat) sobre arrays numpy. Algoritmo (igual
que la implementación de referencia del paper): se baraja el batch para
emparejar cada imagen con otra al azar, se muestrea un único lambda
Beta(1,1) para todo el batch (vía dos Gamma(1,1) con tf.random.gamma,
sin depender de tensorflow_probability), se calcula un recuadro
rectangular aleatorio de área ≈(1-lambda), se recorta esa región de la
imagen pareja y se pega en la imagen original, y las etiquetas se
mezclan en la proporción REAL del área intercambiada (recalculada tras
recortar el recuadro a los bordes de la imagen).

NOTA TÉCNICA — class_weight con etiquetas mezcladas: con CutMix las
etiquetas dejan de ser one-hot puro (pasan a ser p.ej. [0.19, 0.81,
0.00]), así que el parámetro class_weight de Keras —pensado para una
clase por muestra— ya no es aplicable directamente. Para mantener el
mismo espíritu de balanceo que 04_train_cnn_aug.py sin violar la
instrucción de "hiperparámetros idénticos", cada muestra mezclada
recibe un sample_weight = lambda * peso_clase(imagen1) +
(1-lambda) * peso_clase(imagen2), pasado como tercer elemento del
dataset en vez de vía el argumento class_weight de fit() (que no admite
etiquetas continuas).

VALIDACIÓN ANTES DE ENTRENAR: se hizo un smoke test con datos
sintéticos (verificó que las etiquetas mezcladas suman 1.0 y que
sample_weight se calcula correctamente) y otro con datos reales (40
imágenes, 1 época) antes de lanzar el entrenamiento completo — detectó
y corrigió un bug de dtype (float64 vs float32 en la multiplicación de
lambda por las etiquetas, con to_categorical produciendo float64 en
este entorno) que habría hecho fallar el entrenamiento completo si no
se hubiera probado antes. Se generó además muestras_cutmix.png (8
ejemplos de un batch tras CutMix) para verificar visualmente que el
parche rectangular y la mezcla de etiquetas eran correctos ANTES de
confiar en el resultado del entrenamiento — el parche es visible a
simple vista y la etiqueta mezclada coincide con la proporción de área
intercambiada.

ENTRENAMIENTO: se detuvo en la época 13/120 (EarlyStopping, paciencia
12). val_accuracy final tras restore_best_weights: 0.5091. Mirando la
trayectoria completa: la pérdida de validación NUNCA mejora respecto a
su valor en la época 1 (1.0476) — sube y luego solo vuelve a bajar
hasta casi ese mismo nivel (1.0562 en la época 13), sin superarlo nunca.
Dato revelador: la exactitud de validación llega a un pico de 73.3% en
la época 9 — pero EarlyStopping vigila val_loss, no val_accuracy, y el
val_loss de la época 9 (1.0613) es más alto que el de la época 1, así
que restore_best_weights DESCARTA ese pico y se queda con pesos de una
época muy temprana, antes de que el modelo aprendiera a discriminar
clases de verdad.

EVALUACIÓN (06_evaluate.py extendido con un 6º modelo, evaluado sobre
el TEST ORIGINAL sin segmentar —CutMix solo cambia el entrenamiento, no
la distribución de entrada esperada por el modelo—):

Métrica                CNN+Augmentation   CNN+CutMix
Exactitud                    0.5091           0.5091   (IDÉNTICA)
AUC-ROC                       0.7233           0.7175
F1 Macro                       0.2249           0.2249  (IDÉNTICA)
Especificidad Maligno            0.0000           0.0000  (IDÉNTICA)

Matriz de confusión de CNN+CutMix: 100% de las 165 imágenes de test
predichas como "Maligno" (18/18 Benigno mal, 84/84 Maligno por
descarte, 63/63 Normal mal) — el MISMO colapso, celda por celda, que
CNN + Augmentation.

RESPUESTA A LA HIPÓTESIS (explícita, tal como pidió el usuario, sea
cual sea el resultado): NO, CutMix NO evita el colapso. Interpretación:
que dos técnicas de aumento completamente distintas (transformación
geométrica por imagen vs. mezcla de píxeles/etiquetas entre pares)
lleguen al mismo punto muerto, con la misma exactitud exacta (0.5091 =
proporción de la clase mayoritaria "Maligno" en el test) y ambas con
val_loss estancado desde la primera época, sugiere que el colapso no es
un problema del TIPO de aumento sino de cómo interactúan el tamaño
reducido del dataset (767 imágenes de entrenamiento), el desbalance de
clases, y el criterio de selección del mejor modelo
(EarlyStopping(monitor="val_loss")) con cualquier técnica que introduzca
suficiente variabilidad en el entrenamiento: el modelo encuentra el
atajo de predecir siempre la clase mayoritaria porque, bajo esas
condiciones, minimiza val_loss más rápido que aprender a discriminar de
verdad. Es un resultado negativo, documentado tal cual, tal como piden
las reglas globales de esta fase.

Bug de tabla corregido de paso: al regenerar tabla_comparativa.png y
tabla_comparativa_especificidad.png con 5 filas, se notó que el nombre
"Transfer Learning + Segmentación" (añadido en la Tarea 2) se recortaba
visualmente por la izquierda — la columna "Modelo" no se ajustaba al
contenido. Se corrigió añadiendo table.auto_set_column_width(...) en
ambas funciones de 06_evaluate.py (plot_comparison_table y
plot_specificity_table); no afecta a ningún modelo entrenado, solo a
cómo se renderizan las tablas ya calculadas.

Commit y push de esta tarea hechos por separado.

ESTADO ACTUAL: las 3 tareas de la Propuesta 1 (especificidad,
segmentación, CutMix) están completas. Hay 6 modelos evaluados y
comparados: CNN Base, CNN + Augmentation, Transfer Learning, Transfer
Learning + Segmentación, CNN + CutMix (todos con especificidad por
clase, Fase 10 Tarea 1) y SVM (HOG) como línea base sin retocar salvo
la especificidad añadida de forma no invasiva. Los tres objetivos
específicos de la propuesta quedan cumplidos: preprocesamiento con
segmentación (objetivo 1), aumento de datos avanzado con intercambio de
píxeles vía CutMix (mencionado en el resumen), y comparación de
sensibilidad/especificidad con métodos tradicionales (objetivo 3, vía
el baseline SVM+HOG y la tabla de especificidad). Dos de los tres
experimentos nuevos (segmentación, CutMix) dieron resultados negativos
—no mejoraron al modelo original— y se documentaron como tal, en línea
con la instrucción explícita del usuario de que los resultados
negativos son parte válida de la narrativa metodológica del TFM. Queda
UNA sola app web (FastAPI + frontend propio en src/frontend/), con
todas las mejoras de las Fases 6, 8 y 9 (épocas configurables con
límite visible, entrenar todos, log persistente, botón Detener
corregido, rediseño minimalista) y probada de extremo a extremo con
automatización real de navegador (Playwright/Chromium headless), no
solo pruebas manuales. La instalación es un único
"pip install -r requirements.txt", sin conflicto de protobuf. El
proyecto está subido a GitHub: https://github.com/CesarEspa/Maestria


====================================================================
4. LOS 4 MODELOS: ARQUITECTURA Y RESULTADOS DETALLADOS
====================================================================

Todos los modelos de Keras usan class_weight="balanced",
EarlyStopping sobre val_loss (restore_best_weights=True) y
ReduceLROnPlateau. Métricas medidas sobre el conjunto de TEST
(165 imágenes: 18 Benigno, 84 Maligno, 63 Normal), generadas por
06_evaluate.py y 08_baseline_ml.py. Estos son los resultados FINALES,
tras la TERCERA ronda de reentrenamiento descrita en la sección 3
(Fase 8 — mismos hiperparámetros que la Fase 5, pero con el
presupuesto de épocas subido: 80->120 en las CNN, 15->25 y 50->100 en
transfer learning). Para el resultado original de la primera ronda
(antes del diagnóstico y las correcciones), ver sección 5.2.

--------------------------------------------------------------------
4.1 CNN Base (sin aumento de datos) — ARREGLADA POR COMPLETO
--------------------------------------------------------------------
Arquitectura: 3 bloques Conv2D(32->64->128) + LayerNormalization +
MaxPooling + Dropout, GlobalAveragePooling2D, Dense(256), Dense(3,
softmax). ~321,250 parámetros. Learning rate 1e-4.
Loss: CategoricalCrossentropy(label_smoothing=0.1) sobre etiquetas
one-hot. EarlyStopping paciencia 12, hasta 120 épocas (subido de 80 en
la Fase 8).

ÉPOCAS REALES: 34 de 120 (mejor época: 22, val_accuracy 0.861,
val_loss 0.5875 — de la época 23 a la 34, 12 seguidas sin mejorar,
activó EarlyStopping). En la ronda anterior (techo de 80) había
parado en ~38 según lectura visual de la curva; con el techo subido a
120, en realidad terminó parando ANTES (34) porque encontró su mejor
punto más pronto (época 22) — el techo de 80 no era su límite real.

RESULTADO: el colapso de la primera ronda desapareció por completo, y
esta tercera ronda mejoró aún más el resultado de la segunda.
Convergencia normal, sin explosión de la pérdida de validación.

Métricas en test: Exactitud 0.8667 (antes 0.8485), AUC-ROC 0.9449
(antes 0.9386), F1 macro 0.7490 (antes 0.7305).
Sensibilidad: Benigno 0.44 (igual), Maligno 0.99 (igual), Normal 0.83
(antes 0.78).

Esto CONFIRMA la hipótesis de causa raíz planteada en la primera
ronda: el problema no era la arquitectura ni el dataset en
sí, sino la combinación específica de BatchNormalization + pocos
batches/época + learning rate alto. Al sustituir BatchNorm por
LayerNorm (que no depende de estadísticas de batch) y bajar el
learning rate, el mismo modelo converge con normalidad. Además, subir
el presupuesto de épocas en la Fase 8 confirmó que el modelo SÍ
estaba limitado por el techo de 80 de la segunda ronda: con más
margen, EarlyStopping encontró un punto mejor.

--------------------------------------------------------------------
4.2 CNN + Augmentation — SOLO MEJORÓ PARCIALMENTE
--------------------------------------------------------------------
Misma arquitectura y mismos cambios que la CNN Base, más augmentation
reducido (RandomFlip horizontal, RandomRotation 0.08, RandomZoom
0.05, RandomContrast — se quitaron RandomFlip vertical y
RandomTranslation por no ser transformaciones anatómicamente
plausibles/seguras para una TC de tórax).

ÉPOCAS REALES: 13 de 120 (más rápido aún que los 14 de 80 de la ronda
anterior) — la cifra más baja de los 4 modelos: su "mejor" época fue
la propia época 1 (nunca volvió a bajar el val_loss inicial), así que
EarlyStopping cortó en cuanto se cumplieron las 12 épocas de paciencia
sin ninguna mejora. Con el TRIPLE de presupuesto disponible (120 en
vez de 80), el modelo ni siquiera lo intentó más allá del arranque —
confirma con más fuerza que el colapso no es un problema de
presupuesto de épocas, es estructural.

RESULTADO: mixto, e IDÉNTICO a la ronda anterior. El AUC-ROC mejoró
sustancialmente respecto a la primera ronda (de 0.503 a ~0.72), pero
la exactitud final (0.5091) sigue correspondiendo a un colapso:
sensibilidad Benigno 0.00, Maligno 1.00, Normal 0.00 — el modelo
predice "Maligno" para prácticamente todo el test, igual que en la
primera ronda (que colapsaba hacia "Normal" en vez de "Maligno") y
que en la segunda ronda (números calcados: subir el presupuesto de
épocas en la Fase 8 no cambió nada).

Métricas en test (idénticas a la ronda anterior): Exactitud 0.5091,
AUC-ROC 0.7233 (antes 0.7280, diferencia de ruido), F1 macro 0.2249.

INTERPRETACIÓN (importante para el TFM): dado que la CNN Base —misma
arquitectura, mismos hiperparámetros, sin augmentation— SÍ se
arregló con estos cambios, la diferencia entre ambos resultados solo
puede deberse al augmentation en tiempo real. Esto es evidencia de
que el augmentation, incluso reducido, es un factor causal adicional
de inestabilidad en este dataset pequeño (24 batches/época): añade
variabilidad extra que, combinada con el desbalance de clases, sigue
empujando al optimizador hacia un mínimo de "predecir la clase
mayoritaria". Que el AUC mejore sin que la exactitud lo haga sugiere
que el modelo SÍ aprende información útil (mejor ranking de
probabilidades) pero el umbral de decisión final (argmax) no llega a
cruzarse hacia las otras clases — posible línea de trabajo futuro:
ajustar umbrales de decisión por clase en vez de usar argmax puro.

Siguiendo instrucción explícita del usuario, este resultado NO se
persiguió más allá de esta ronda: se documenta como hallazgo válido
("se intentó, mejoró parcialmente, el colapso final persiste,
probablemente por el augmentation") en vez de seguir iterando.

--------------------------------------------------------------------
4.3 Transfer Learning (EfficientNetB0) — MEJORÓ EN LA 2DA RONDA, EMPEORÓ EN LA 3RA
--------------------------------------------------------------------
EfficientNetB0 preentrenada en ImageNet + cabeza personalizada
(GlobalAveragePooling2D, Dropout, Dense(256), Dropout, Dense(3,
softmax)). Fase 1: base congelada, solo entrena la cabeza (learning
rate 1e-4, hasta 25 épocas, subido de 15 en la Fase 8). Fase 2:
fine-tuning de las ÚLTIMAS 50 CAPAS de EfficientNet (learning rate
1e-5, hasta 100 épocas, subido de 50 en la Fase 8).

ÉPOCAS REALES (Fase 8, ronda actual): Fase 1 paró en la época 9 de 25
(igual que antes con el techo de 15 — no estaba limitada por el
presupuesto). Fase 2 llegó a la época 52 de 100 (mejor punto: época 40
de la fase, val_accuracy 0.830, val_loss 0.499) — SÍ aprovechó el
presupuesto extra (52 frente a 50 antes), pero el resultado en test
fue peor, no mejor (ver más abajo). Total: 61 épocas efectivas (9+52).

RESULTADO SEGUNDA RONDA (histórico, techo 15+50): mejoró en todas las
métricas respecto a la primera ronda. Exactitud 0.8303, AUC-ROC
0.9761, F1 macro 0.7764, sensibilidad Benigno 0.89 (la mejora más
notable, en la clase con menos datos), Maligno 0.87, Normal 0.76.

RESULTADO TERCERA RONDA (ACTUAL, techo 25+100): empeoró pese a usar
más épocas reales. Exactitud 0.7939 (antes 0.8303), AUC-ROC 0.9563
(antes 0.9761), F1 macro 0.7330 (antes 0.7764). Sensibilidad: Benigno
0.83 (antes 0.89), Maligno 0.86 (antes 0.87), Normal 0.70 (antes
0.76). Las métricas de VALIDACIÓN del punto que EarlyStopping eligió
como mejor fueron similares o incluso mejores que las de la ronda
anterior — la caída aparece solo al evaluar sobre TEST.

INTERPRETACIÓN DEL EMPEORAMIENTO (importante, no es un error): con
val y test de solo 165 imágenes cada uno, ambos conjuntos son pequeños
y su composición concreta pesa más de lo ideal en las métricas.
EarlyStopping elige el punto de parada según val_loss, no según test:
cuando ambos splits son pequeños, "mejor en validación" no garantiza
"mejor en test" en la misma proporción. Es evidencia práctica (no solo
teórica) de que un único split fijo de este tamaño no basta para
conclusiones robustas sobre qué modelo es mejor — refuerza la
recomendación de validación cruzada (k-fold) en trabajo futuro
(sección 7).

Hipótesis sin descartar del todo: con más capas descongeladas y más
épocas de fine-tuning, el modelo tiene más capacidad de adaptar las
representaciones preentrenadas de ImageNet al dominio específico de TC
de tórax — pero eso no evitó, en esta ronda, que el punto elegido por
EarlyStopping generalizara peor al test.

¿CUÁL ES "EL MEJOR MODELO" AHORA? Con los números de la tercera ronda,
es una decisión más reñida que antes:
  - CNN Base tiene ahora la exactitud MÁS ALTA (86.67% vs 79.39%) Y el
    F1 macro más alto (0.749 vs 0.733) — se invirtió respecto a la
    segunda ronda, donde Transfer Learning ganaba en F1.
  - Transfer Learning conserva el AUC-ROC más alto (0.956 vs 0.945) y,
    sobre todo, sensibilidad mucho más equilibrada entre clases,
    incluida la minoritaria Benigno (0.83 vs 0.44 de CNN Base — CNN
    Base SIGUE sin detectar más de la mitad de los casos benignos,
    pese a su mejor exactitud global).
  - En un problema médico, ese desequilibrio de sensibilidad pesa más
    que un punto de exactitud. RECOMENDACIÓN: Transfer Learning sigue
    siendo el modelo principal recomendado por motivos clínicos, pero
    la diferencia con CNN Base ya no es tan clara como en la segunda
    ronda — ambos resultados (el que mejoró y el que empeoró) se
    reportan tal cual en el TFM, es un hallazgo legítimo sobre las
    limitaciones de evaluar con datasets pequeños, no algo a ocultar.

--------------------------------------------------------------------
4.4 SVM (HOG) — Línea base de ML clásico — SIN CAMBIOS (no se tocó)
--------------------------------------------------------------------
Extracción de características HOG (9 orientaciones, celdas 16x16,
bloques 2x2) sobre la imagen en escala de grises, + SVM kernel RBF
con class_weight="balanced"). Por instrucción explícita del usuario,
este modelo NO se reentrenó ni se modificó en la segunda ronda. En la
Fase 8 (tercera ronda) sí se volvió a ejecutar por completo (para
tener los 4 modelos frescos), pero al ser un método determinista, sin
concepto de épocas, produjo un archivo .joblib byte a byte idéntico
al anterior — mismo resultado, sin cambios.

ÉPOCAS: no aplica. Un SVM no se entrena por descenso de gradiente
iterativo por épocas — resuelve un problema de optimización convexa
con una única llamada a .fit(), converge (o falla) de una vez.

RESULTADO (idéntico a la primera ronda): exactitud, AUC-ROC y F1
PERFECTOS (1.00) en el conjunto de test. Sensibilidad 1.00 en las 3
clases.

ESTO NO ES UN ÉXITO, ES UNA SEÑAL DE ALARMA. Para un clasificador
clásico sin aprendizaje de representaciones, en un problema médico
de 3 clases, un resultado perfecto no es creíble como capacidad de
generalización real. Es la evidencia empírica más clara del problema
de fuga de datos descrito en la sección 2: al no existir
identificador de paciente, es muy probable que cortes de TC casi
idénticos del mismo paciente aparezcan tanto en entrenamiento como
en prueba, permitiendo que el modelo memorice la apariencia de un
paciente en vez de aprender patrones patológicos generalizables.

Este resultado se reporta íntegramente por transparencia
metodológica, pero NO debe leerse como que "SVM + HOG supera a las
CNN". La comparación correcta, imposible de hacer con los datos
disponibles, requeriría un split a nivel de paciente.

--------------------------------------------------------------------
4.6 Transfer Learning + Segmentación (Fase 10, Tarea 2) — NO MEJORÓ
--------------------------------------------------------------------
Mismo modelo e hiperparámetros que 4.3 (EfficientNetB0, 50 capas
descongeladas, mismo LR y presupuesto de épocas), entrenado sobre
dataset_splits_segmented.npz (fondo y mesa del escáner eliminados por
segmentación clásica, ver Fase 10/Tarea 2 en la sección 3) en vez del
dataset original. Evaluado sobre el TEST SEGMENTADO (no el original,
para que la distribución de entrada coincida con la de entrenamiento).

RESULTADO: empeoró frente al modelo sin segmentar en casi todo.
Exactitud 0.7879 (vs 0.7939, casi igual), pero AUC-ROC 0.9118 (vs
0.9563, bajó) y F1 macro 0.6968 (vs 0.7330, bajó). La caída más
importante: sensibilidad Benigno 0.5556 (vs 0.8333) — se pierde gran
parte de la mejora que hacía valioso al modelo original. Sensibilidad
Maligno sube (0.8929 vs 0.8571) y Normal sube levemente (0.7143 vs
0.6984). Especificidad: Benigno sube (0.8571 vs 0.8299), pero Maligno
(0.9630 vs 1.0000) y Normal (0.8922 vs 0.9118) bajan.

HIPÓTESIS: eliminar el fondo y la mesa del escáner también elimina
contexto (posición/forma del cuerpo, caja torácica) que la red venía
aprovechando de forma implícita; además la segmentación clásica no es
perfecta y añade su propio ruido (bordes irregulares, recorte parcial
de masas grandes que tocan el borde pulmonar — ver
ejemplos_segmentacion.png y el caso Maligno en el prototipo con notch
visible en la máscara).

GRAD-CAM: el artefacto de la esquina saturada (sección 5.3) DESAPARECE
en este modelo, pero es reemplazado por un degradado izquierda-derecha
que ignora la forma de la máscara pulmonar — y que, verificado con
correlación de Pearson entre mapas de calor crudos de 3 imágenes de
contenido distinto, es prácticamente constante (r=0.999). La
segmentación cambió la FORMA del problema de explicabilidad, no lo
resolvió — ver el detalle completo en la Fase 10/Tarea 2 (sección 3).

CONCLUSIÓN: resultado negativo, documentado íntegramente tal como
pidió el usuario. El objetivo específico 1 de la propuesta (implementar
segmentación) queda cumplido porque se implementó, validó y evaluó con
rigor — el hallazgo de que este enfoque clásico concreto no mejora el
modelo es en sí mismo un resultado válido para el TFM, no un fallo del
proceso.

--------------------------------------------------------------------
4.7 CNN + CutMix (Fase 10, Tarea 3) — REPRODUCE EL COLAPSO NÚMERO POR NÚMERO
--------------------------------------------------------------------
Misma arquitectura, optimizador, loss, presupuesto de épocas, paciencia
y class_weight (adaptado a sample_weight, ver Fase 10/Tarea 3 en la
sección 3) que 4.2 (CNN + Augmentation). Único cambio: CutMix en vez de
augmentation geométrico — recorta una región rectangular de una imagen
del batch y la pega sobre otra, mezclando las etiquetas en proporción
al área intercambiada. Evaluado sobre el TEST ORIGINAL sin segmentar
(CutMix no cambia la distribución de entrada esperada por el modelo).

RESULTADO: colapso idéntico al de CNN + Augmentation. Exactitud 0.5091
(exactamente igual), AUC-ROC 0.7175 (vs 0.7233, muy similar), F1 macro
0.2249 (exactamente igual). Matriz de confusión: 100% de las 165
imágenes de test predichas como "Maligno" — mismo patrón de fallo,
celda por celda.

ENTRENAMIENTO: se detuvo en la época 13/120 (paciencia 12). El val_loss
nunca mejora respecto a su valor en la época 1 (1.0476); hay un pico de
val_accuracy de 73.3% en la época 9 que EarlyStopping descarta porque
vigila val_loss (más alto en la época 9 que en la época 1), no
val_accuracy — restore_best_weights se queda con pesos de una época muy
temprana.

CONCLUSIÓN: la hipótesis de que mezclar etiquetas de forma continua
evitaría el colapso NO se confirma. Que dos técnicas de aumento
completamente distintas lleguen al mismo punto muerto, con la misma
exactitud EXACTA (0.5091 = proporción de la clase mayoritaria en el
test) sugiere que el problema no es el TIPO de aumento sino la
interacción entre el tamaño reducido del dataset, el desbalance de
clases, y el monitor="val_loss" de EarlyStopping con cualquier técnica
que introduzca suficiente variabilidad de entrenamiento. Resultado
negativo, documentado íntegramente tal como pidió el usuario.

--------------------------------------------------------------------
4.8 Tabla resumen (resultados FINALES, tercera ronda — Fase 8; especificidad, 5º y 6º modelo añadidos en la Fase 10)
--------------------------------------------------------------------
Modelo                     Exactitud  AUC-ROC  F1 Macro  Sens.Benigno Sens.Maligno Sens.Normal
CNN Base (arreglada)         0.8667    0.9449   0.7490    0.44         0.99         0.83
CNN + Augmentation (colapsa) 0.5091    0.7233   0.2249    0.00         1.00         0.00   <- idéntica a la 2da ronda
Transfer Learning (EffNet)   0.7939    0.9563   0.7330    0.83         0.86         0.70   <- modelo recomendado
Transfer + Segmentación      0.7879    0.9118   0.6968    0.56         0.89         0.71   <- NO mejoró al original (Fase 10/Tarea 2)
CNN + CutMix (colapsa)       0.5091    0.7175   0.2249    0.00         1.00         0.00   <- reproduce el colapso de CNN+Aug (Fase 10/Tarea 3)
SVM (HOG) linea base         1.0000    1.0000   1.0000    1.00         1.00         1.00   <- sospechoso de leakage, sin cambios

Especificidad por clase (uno-contra-el-resto, ver Fase 10 para la fórmula):
Modelo                     Espec.Benigno  Espec.Maligno  Espec.Normal  Espec.Macro
CNN Base                     0.9184         1.0000         0.9020        0.9401
CNN + Augmentation            1.0000         0.0000         1.0000        0.6667   <- 0.00 en Maligno: nunca descarta a nadie de esa clase
Transfer Learning             0.8299         1.0000         0.9118        0.9139
Transfer + Segmentación        0.8571         0.9630         0.8922        0.9041
CNN + CutMix                    1.0000         0.0000         1.0000        0.6667   <- mismo 0.00 en Maligno que CNN+Aug
SVM (HOG) linea base              1.0000         1.0000         1.0000        1.0000

Comparación con la segunda ronda (techo de épocas 80/50, sección 4 de
versiones anteriores de este documento):
Modelo                     Exactitud 2da  Exactitud 3ra  Cambio
CNN Base                     0.8485         0.8667        MEJORÓ
CNN + Augmentation            0.5091         0.5091        IGUAL (colapso idéntico)
Transfer Learning             0.8303         0.7939        EMPEORÓ (ver 4.3 para el porqué)
SVM (HOG)                     1.0000         1.0000        IGUAL (determinista)

Figuras nuevas generadas en la tercera ronda (en outputs/figures/):
  - curvas_roc_comparativas.png: las curvas ROC macro-average
    (one-vs-rest) superpuestas en una sola figura, con AUC en la
    leyenda. La línea de la SVM es prácticamente plana en TPR=1.0,
    lo cual ilustra visualmente lo implausible de su resultado.
  - classification_report_<modelo>.png: tabla de precision/recall/
    f1-score/support por clase, como imagen, para cada modelo
    (incluida la SVM, en modo solo lectura).

Figuras nuevas generadas en la Fase 10 (especificidad, segmentación,
CutMix — en outputs/figures/ salvo donde se indica):
  - tabla_comparativa_especificidad.png, sensibilidad_especificidad.png
  - ejemplos_segmentacion.png, curvas_transfer_learning_segmentado.png,
    confusion_matrix_transfer_learning_segmentacion.png,
    classification_report_transfer_learning_segmentacion.png,
    outputs/gradcam/gradcam_grid_segmentado.png
  - muestras_cutmix.png, curvas_cnn_cutmix.png,
    confusion_matrix_cnn_cutmix.png, classification_report_cnn_cutmix.png


====================================================================
5. INCIDENCIAS TÉCNICAS ENCONTRADAS Y RESUELTAS DURANTE EL DESARROLLO
====================================================================
(Útil para la sección de Metodología del TFM: demuestra rigor en el
proceso, no solo en el resultado final.)

5.1 Bug real: augmentation activa incluso en evaluación/inferencia
--------------------------------------------------------------------
En 04_train_cnn_aug.py y 05_train_transfer.py, la capa de
augmentation se conectaba con training=True fijado explícitamente al
construir el grafo del modelo. En Keras, cuando se fija ese
argumento explícitamente en la llamada a una capa, ese valor queda
"clavado" para ese nodo del grafo y YA NO sigue el modo del modelo
completo — es decir, la augmentation se seguía aplicando incluso
dentro de model.evaluate() o model.predict(), no solo durante el
entrenamiento. Se corrigió quitando ese argumento explícito (para
que el modo se propague correctamente desde la llamada al modelo).

Impacto medido: en la CNN+Augmentation no cambió el resultado (el
colapso se reprodujo igual). En Transfer Learning SÍ mejoró de forma
medible: AUC-ROC subió de 0.907 a 0.913, y la sensibilidad en la
clase minoritaria (Benigno) subió de 0.11 a 0.50 — evidencia de que
evaluar con augmentation activo introducía ruido que perjudicaba
especialmente a la clase con menos ejemplos.

5.2 Bug real: Grad-CAM no funcionaba con el modelo de transfer learning
--------------------------------------------------------------------
En Keras 3, reconstruir un keras.Model recortado desde model.inputs
hasta la salida de un submodelo anidado (la base de EfficientNet
dentro del modelo de transfer learning) falla con el error "Output
with path 0 is not connected to inputs". Se solucionó reproduciendo
el forward pass manualmente, capa por capa, dentro de un
GradientTape, en vez de intentar reconstruir el grafo (ver
gradcam_utils.py).

5.3 Hallazgo tras corregir 5.2: Grad-CAM en transfer learning no es informativo
--------------------------------------------------------------------
Una vez arreglado el bug técnico, se encontró que el mapa de calor
crudo de Grad-CAM para el modelo de transfer learning es EXACTAMENTE
CERO en 48 de las 49 celdas de la rejilla 7x7 de activaciones, con
un único píxel (siempre en la misma esquina) saturado a 1.0 — este
patrón se repite idéntico para imágenes de entrada completamente
distintas. Es decir, el mapa Grad-CAM en este modelo NO es
informativo: no resalta regiones anatómicas relevantes.

Explicación más plausible: un fenómeno conocido en redes
convolucionales/transformers preentrenadas, donde ciertos canales
desarrollan activaciones de magnitud anormalmente alta y
aproximadamente constantes en posiciones fijas (a menudo por efectos
de padding en convoluciones con stride), que dominan el promedio
ponderado por gradiente de Grad-CAM y eclipsan las contribuciones
más pequeñas pero semánticamente relevantes del resto del mapa. Es
un límite conocido del Grad-CAM "clásico" frente a ciertas
arquitecturas preentrenadas, no un error de implementación.

IMPLICACIÓN PARA EL TFM: la explicabilidad de este sistema mediante
Grad-CAM es limitada para el mejor modelo. Esto debe mencionarse
honestamente en la sección de resultados/discusión, y como línea de
trabajo futuro (probar Grad-CAM++, o excluir/normalizar canales
atípicos antes de generar el mapa).

VERIFICACIÓN EN LA SEGUNDA RONDA: se regeneró el grid de Grad-CAM con
el modelo de transfer learning reentrenado (50 capas descongeladas,
83.0% de exactitud) y el mismo patrón de saturación en una esquina
persiste, sin cambios. Confirma que no depende del punto de
entrenamiento concreto, sino de una propiedad más estructural de
cómo EfficientNetB0 representa la información en su última capa.

5.4 Bug real: el botón "Detener" no reflejaba el estado hasta 5 minutos después
--------------------------------------------------------------------
kill_process() (training_control.py) lanzaba "taskkill /F /T" contra
el PID del subproceso de entrenamiento y devolvía éxito de inmediato,
SIN comprobar que el proceso realmente hubiera muerto. Además, el
endpoint POST /api/models/{key}/stop (api.py) nunca actualizaba el
JSON de progreso tras matar el proceso. Resultado: el archivo de
progreso se quedaba con status="running" y datos congelados, así que
la tarjeta en el frontend seguía mostrando "Entrenando..." con la
barra sin avanzar hasta que pasaban los 5 minutos del umbral de
"stale" (sin actividad reciente) — parecía que la app se hubiera
colgado, sin serlo.

Corrección: kill_process() ahora espera hasta 8 segundos comprobando
activamente (tasklist en Windows) que el PID ya no exista antes de
devolver éxito; y el endpoint /stop llama a mark_status(key, ...,
"stopped") en cuanto se confirma la muerte del proceso, para que el
frontend refleje "Detenido" al instante. Se añadió el estado
"stopped" a STATUS_BADGES y a la lógica de effective_status(), y el
frontend lo trata como estado terminal (igual que completed/failed)
para habilitar de nuevo el botón "Reentrenar". Verificado con un
ciclo real completo (lanzar con pocas épocas, esperar a que arrancara,
pulsar Detener): pasó de "Entrenando..." a "Detenido" en
aproximadamente 1 segundo, con el proceso genuinamente terminado
(confirmado revisando la lista de procesos vivos, no solo el código
de retorno).

De paso se corrigió una fuga de recursos relacionada: launch_training()
abría el archivo de log del proceso y nunca lo cerraba, acumulando un
descriptor de archivo abierto por cada entrenamiento lanzado durante
toda la vida del proceso servidor (relevante porque este backend está
pensado para quedarse corriendo mucho tiempo). Se cierra ahora
inmediatamente después de Popen() (seguro en Windows: Popen duplica
el handle para el proceso hijo).

5.5 Otros problemas menores resueltos
--------------------------------------------------------------------
  - UnicodeEncodeError en Windows: la consola por defecto (cp1252)
    no puede imprimir caracteres como flechas o checks que usan los
    scripts. Se soluciona fijando PYTHONIOENCODING=utf-8 antes de
    ejecutar cualquier script.
  - Conflicto de dependencias real entre TensorFlow y Streamlit:
    TensorFlow exige protobuf<5, Streamlit exige protobuf>=5.26.1 —
    son metadatos mutuamente excluyentes. Un primer intento de
    fijarlo con una sola línea "protobuf==X" DENTRO del mismo
    requirements.txt que streamlit/fastapi falla con
    "ResolutionImpossible" al probarlo de verdad en un venv limpio
    (pip valida conflictos entre paquetes de una misma invocación).
    La solución que sí funciona, verificada de extremo a extremo en
    un venv nuevo: separar en dos archivos —requirements.txt (pipeline)
    y requirements-web.txt (streamlit/fastapi)— e instalarlos en
    INVOCACIONES DE PIP SEPARADAS, seguido de "pip install
    protobuf==4.25.9" como tercer paso. En la práctica esa versión de
    protobuf funciona bien en tiempo de ejecución con ambas librerías
    pese al conflicto declarado en sus metadatos.
    RESUELTO DEL TODO EN LA FASE 9: al eliminar la app Streamlit (la
    única que exigía protobuf>=5 — confirmado con "pip show protobuf",
    que lista streamlit como el único paquete instalado que lo exige),
    el conflicto desapareció por completo. Ya no hace falta el pin
    manual ni los 3 pasos separados: un solo requirements.txt con
    tensorflow+fastapi+uvicorn se instala en un solo comando, sin
    avisos de conflicto, verificado en un venv nuevo.
  - Nombre de archivo con doble guion bajo (bug cosmético) en
    06_evaluate.py al generar el nombre del archivo de la matriz de
    confusión de "CNN + Augmentation"; corregido.
  - Se descubrió, mientras se probaba la app con pruebas
    automatizadas, que mover/eliminar la carpeta de progreso
    (outputs/progress/) MIENTRAS un entrenamiento real está
    corriendo hacía que el entrenamiento se cayera con
    FileNotFoundError. Se corrigió progress_tracker.py para que
    cualquier fallo al escribir el progreso (que es solo para la
    interfaz) se ignore en silencio y NUNCA interrumpa el
    entrenamiento real del modelo.
  - GPU no utilizable pese a estar presente: el equipo tiene una GPU
    NVIDIA física (RTX 3050, 4 GB), confirmada por nvidia-smi, pero
    tf.config.list_physical_devices('GPU') devuelve una lista vacía.
    Causa: desde TensorFlow 2.11, "pip install tensorflow" ya no trae
    soporte nativo de GPU/CUDA en Windows. Se necesitaría WSL2 o el
    plugin tensorflow-directml-plugin de Microsoft (con riesgo de
    tener que bajar de versión TensorFlow y romper el código Keras 3
    ya escrito y probado). Se decidió NO intentarlo por el riesgo de
    dejar el entorno roto sin garantía de éxito, y reentrenar en CPU
    en su lugar (funcionó, solo más lento). Pendiente como mejora de
    infraestructura si se quiere iterar más rápido en el futuro.


====================================================================
6. LIMITACIONES (para la sección correspondiente del TFM)
====================================================================

  1. FUGA DE DATOS POR AUSENCIA DE IDENTIFICADOR DE PACIENTE (la más
     importante — ver sección 2). El split se hizo a nivel de imagen,
     no de paciente, lo que probablemente infla las métricas de
     generalización, especialmente las de la SVM.
  2. Tamaño de dataset reducido para deep learning (1097 imágenes,
     767 para entrenamiento): favorece overfitting y limita la
     capacidad de las arquitecturas entrenadas desde cero.
  3. Desbalance de clases (Benigno: 10.9% del total). Mitigado con
     class_weight, pero sin remuestreo ni generación sintética
     adicional.
  4. Un único origen de datos (hospitales de Irak, dataset de 2019).
     No se evaluó generalización a otros equipos/protocolos/
     poblaciones.
  5. Entrenamiento en CPU en ambas rondas (GPU física presente pero no
     utilizable por limitaciones de TensorFlow en Windows, ver sección
     5.4): se pudo iterar dos veces sobre los hiperparámetros de las
     CNN entrenadas desde cero (con éxito total en CNN Base, parcial
     en CNN+Augmentation — ver sección 4.1-4.2), pero una búsqueda de
     hiperparámetros más exhaustiva habría sido más rápida con GPU.
  6. No es un dispositivo médico. Prototipo académico sin validación
     clínica, no certificado, no debe usarse para decisiones de
     diagnóstico o tratamiento reales.


====================================================================
7. TRABAJO FUTURO (líneas a mencionar en conclusiones)
====================================================================

  - Reconstruir un split a nivel de PACIENTE (requiere acceso a
    metadatos DICOM originales, no solo a las imágenes exportadas de
    Kaggle) y volver a medir todas las métricas — se espera una
    caída sustancial de exactitud, especialmente en la SVM.
  - Investigar específicamente por qué el aumento de datos en tiempo
    real sigue causando colapso en la CNN, ya sea con augmentation
    geométrico o con CutMix, aunque la misma arquitectura sin ningún
    aumento (CNN Base) ya no colapsa con los mismos hiperparámetros
    (ver secciones 4.2 y 4.7) — probar augmentation/CutMix más suave
    (alpha más bajo en CutMix), aplicarlo solo a partir de cierta época
    (curriculum), o cambiar EarlyStopping para vigilar val_accuracy en
    vez de val_loss (la sección 4.7 documenta un pico de 73.3% de
    val_accuracy en CutMix que se descarta precisamente por vigilar
    val_loss).
  - Probar umbrales de decisión por clase en vez de argmax puro para
    la CNN+Augmentation y la CNN+CutMix: sus AUC-ROC (0.723 y 0.718)
    sugieren que ambos modelos aprenden información útil aunque la
    decisión final colapse.
  - Configurar soporte de GPU real (WSL2 o DirectML) para poder
    iterar más rápido sobre estas hipótesis (ver limitación 5).
  - Validación cruzada (k-fold) en vez de un único split, dado el
    tamaño reducido del dataset — la Fase 8 (tercera ronda) ya dio
    evidencia directa de esta necesidad: Transfer Learning bajó de
    exactitud en test pese a mejores métricas de validación, señal de
    que un único split de 165+165 imágenes no es suficientemente
    estable para conclusiones robustas sobre "cuál modelo es mejor".
  - Evaluar en un conjunto externo (otro hospital/dataset público)
    para medir generalización real.
  - Explorar arquitecturas 3D que aprovechen la información
    volumétrica de cortes consecutivos, en vez de clasificar cortes
    2D de forma independiente.
  - Mejorar la explicabilidad: probar Grad-CAM++ u otras variantes
    menos sensibles a canales de activación atípica (ver 5.3) — el
    patrón persiste (con otra forma) incluso tras segmentar (Fase 10),
    así que el problema no parece estar en el fondo de la imagen.
  - Probar segmentación pulmonar APRENDIDA (una U-Net sobre máscaras
    anotadas) en vez del enfoque clásico Otsu + morfología de la Fase
    10 — el enfoque clásico no mejoró el modelo, pero no descarta que
    una segmentación más precisa sí pudiera hacerlo.
  - La app Streamlit se eliminó en la Fase 9 por quedar redundante
    frente a la de FastAPI (ver esa sección para el detalle). Si en
    algún momento hiciera falta una segunda interfaz más simple,
    evaluar reconstruirla — pero manteniendo una sola interfaz como
    fuente de verdad, para no repetir la divergencia que llevó a
    eliminar la anterior.


====================================================================
8. QUÉ HAY DISPONIBLE COMO EVIDENCIA / MATERIAL PARA EL DOCUMENTO
====================================================================

  outputs/figures/          Gráficos EDA, ejemplos de segmentación
                             pulmonar, ejemplos de mezcla CutMix, curvas
                             de entrenamiento, matrices de confusión,
                             tablas comparativas (sensibilidad y
                             especificidad por separado), gráfico de
                             barras sensibilidad vs especificidad,
                             curvas ROC comparativas de los 6 modelos,
                             classification report de cada modelo como
                             imagen, y JSON con todas las métricas
                             exactas.
  outputs/gradcam/           Grid de Grad-CAM del mejor modelo
                             (gradcam_grid.png) y de su variante
                             segmentada (gradcam_grid_segmentado.png),
                             9 ejemplos cada uno, 3 por clase.
  outputs/models/            Los 6 modelos entrenados guardados en
                             disco (.keras y .joblib), incluidos
                             transfer_efficientnet_segmented.keras y
                             cnn_cutmix.keras.
  README.md                  Documentación técnica completa: cómo
                             instalar, ejecutar el pipeline y usar
                             la app web, con la misma tabla de
                             resultados y el mismo análisis de
                             hallazgos que este archivo.

Para escribir las secciones de Resultados y Discusión del TFM, la
sección 4 de este archivo (con las 4 tablas/análisis por modelo) es
el material base directo. Para Metodología, usar la sección 3
(pipeline) y las arquitecturas descritas en 4.1-4.4. Para
Limitaciones, usar la sección 6 tal cual. Para Conclusiones, la
sección 7 más el hallazgo central de la tercera ronda (Fase 8): la
comparación entre entrenar desde cero (CNN Base) y transfer learning
ya NO es unilateral — CNN Base tiene ahora mejor exactitud y F1 macro,
pero Transfer Learning mantiene mejor AUC-ROC y una sensibilidad mucho
más equilibrada entre clases (crucial en un problema médico). Esa
mezcla de resultados, incluido el que empeoró (Transfer Learning en la
Fase 8), es en sí misma una conclusión metodológica válida sobre las
limitaciones de evaluar con datasets pequeños de imagen médica.


====================================================================
9. GUION REDACTADO, LISTO PARA ADAPTAR AL DOCUMENTO DEL TFM
====================================================================

Todo lo de abajo está escrito en el registro formal de un TFM (no en
el tono de bitácora del resto de este archivo), organizado por
capítulos típicos de memoria de máster, para poder copiarlo y
adaptarlo directamente al documento final. Los números y nombres de
archivo son exactos a fecha de la Fase 9; si se vuelve a entrenar
algún modelo, hay que releer la sección 4 antes de dar estos párrafos
por buenos (ver la Fase 7 sobre por qué no asumirlo sin comprobar).

--------------------------------------------------------------------
9.1 Introducción
--------------------------------------------------------------------
El cáncer de pulmón es, según la Organización Mundial de la Salud,
una de las principales causas de mortalidad por cáncer a nivel
mundial, en gran parte porque suele diagnosticarse en etapas
avanzadas, cuando las opciones de tratamiento son más limitadas y el
pronóstico empeora considerablemente. La tomografía computarizada
(TC) de tórax es una de las herramientas de imagen más utilizadas
para la detección y el seguimiento de nódulos pulmonares, pero su
interpretación depende en gran medida de la experiencia del
radiólogo, del volumen de estudios que debe revisar y de la
variabilidad inherente entre observadores. En este contexto, los
sistemas de apoyo al diagnóstico asistido por computador (conocidos
como CAD, por sus siglas en inglés — CADe para detección y CADx para
caracterización/clasificación) han cobrado relevancia como una
segunda opinión automatizada que puede ayudar a priorizar casos,
reducir la carga de trabajo y disminuir el riesgo de pasar por alto
hallazgos relevantes, sin que esto implique sustituir el criterio
clínico del especialista.

En paralelo, el aprendizaje profundo —y en particular las redes
neuronales convolucionales (CNN)— se ha consolidado como el enfoque
de referencia para tareas de clasificación de imágenes médicas,
gracias a su capacidad de aprender representaciones jerárquicas
directamente de los píxeles, sin necesidad de diseñar manualmente
características. Sin embargo, entrenar una CNN desde cero requiere
grandes volúmenes de datos etiquetados, un recurso escaso en imagen
médica por razones de privacidad, coste de anotación y la propia
rareza de ciertas condiciones. Dos estrategias habituales para mitigar
esta limitación son el aumento de datos (*data augmentation*), que
genera variaciones sintéticas de las imágenes de entrenamiento para
exponer al modelo a más variabilidad sin necesidad de más pacientes,
y el aprendizaje por transferencia (*transfer learning*), que
reutiliza las representaciones visuales aprendidas por una red
preentrenada en un dominio con muchos datos (típicamente ImageNet) y
las adapta a un dominio médico con muchos menos ejemplos disponibles.

Este Trabajo de Fin de Máster (TFM) explora precisamente esa
disyuntiva: comparar, sobre un mismo dataset público y reducido de TC
de tórax (IQ-OTH/NCCD, 1097 imágenes en tres categorías: Normal,
Benigno y Maligno), tres enfoques de clasificación basados en CNN
—una red convolucional simple entrenada desde cero, la misma red con
aumento de datos en tiempo real, y un modelo de transfer learning
sobre EfficientNetB0— frente a una línea base de aprendizaje
automático clásico (HOG + SVM), evaluando no solo qué tan bien
clasifica cada modelo, sino también qué tan fiable es esa evaluación
y qué tan explicable resulta la decisión del modelo mejor posicionado
mediante mapas de activación Grad-CAM. A lo largo del desarrollo se
tomó la decisión metodológica de documentar con la misma honestidad
los resultados favorables y los que no lo fueron —incluyendo un
colapso de entrenamiento diagnosticado y corregido a mitad de
proyecto, una señal de fuga de datos detectada en la línea base
clásica, y una limitación de explicabilidad encontrada en el mejor
modelo—, en la convicción de que ese rigor es tan parte de los
resultados de un TFM como las cifras de exactitud.

--------------------------------------------------------------------
9.2 Objetivos
--------------------------------------------------------------------

OBJETIVO GENERAL
Desarrollar y evaluar un sistema de clasificación automática de
imágenes de TC de tórax en las categorías Normal, Benigno y Maligno,
basado en redes neuronales convolucionales y técnicas de aumento de
datos, orientado al apoyo del proceso de cribado en servicios de
radiología.

Este objetivo general se considera CUMPLIDO: se construyó un pipeline
completo (preprocesamiento → entrenamiento → evaluación →
explicabilidad) y una aplicación web funcional que permite entrenar,
clasificar y visualizar resultados de extremo a extremo, con cuatro
modelos comparables entre sí sobre el mismo conjunto de prueba. El
matiz importante, que se desarrolla en la sección de Limitaciones, es
que la propia evaluación reveló una limitación estructural del
dataset (ausencia de identificador de paciente) que impide afirmar
con total confianza que las métricas obtenidas reflejen capacidad de
generalización clínica real — algo que, lejos de invalidar el
objetivo, forma parte de una evaluación crítica honesta.

OBJETIVOS ESPECÍFICOS Y SU CUMPLIMIENTO

  1. Preprocesar una base de datos de tomografías computarizadas
     implementando técnicas de segmentación y aumento de datos
     (normalización, partición estratificada 70/15/15, segmentación,
     data augmentation).

     CUMPLIDO. Se implementó un análisis exploratorio (01_eda.py) que
     caracteriza la distribución de clases (120 Benigno / 561 Maligno
     / 416 Normal — un desbalance de clase relevante, con Maligno
     representando el 51.1% del total y Benigno solo el 10.9%), la
     distribución de tamaños originales de imagen y la distribución
     de intensidad de píxel por clase (ver figuras explicadas en 9.5).
     El preprocesamiento (02_preprocessing.py) normalizó todas las
     imágenes a 224×224 px con interpolación LANCZOS, las escaló a
     [0, 1], y generó una partición estratificada 70% entrenamiento
     (767 imágenes) / 15% validación (165) / 15% prueba (165) con
     semilla fija para reproducibilidad. El desbalance de clases se
     compensó con `class_weight="balanced"` en todos los modelos de
     Keras, y se implementó aumento de datos en tiempo real (flip
     horizontal, rotación, zoom, contraste) para uno de los tres
     modelos CNN, evaluando su efecto de forma controlada frente a la
     misma arquitectura sin augmentation. Adicionalmente (02b_segmentation.py)
     se implementó segmentación pulmonar clásica (Otsu + morfología
     matemática, sin deep learning) que aísla los campos pulmonares
     eliminando fondo, mesa del escáner y estructuras óseas; se
     reentrenó el modelo recomendado (Transfer Learning) sobre estos
     datos para medir el efecto real de segmentar — el resultado fue
     negativo (no mejoró exactitud, AUC-ROC ni F1, y la sensibilidad
     en Benigno bajó notablemente), un hallazgo honesto que se
     documenta íntegramente en vez de ocultarse (ver 9.4 y 4.6). Se
     implementó además una técnica de aumento de datos avanzada,
     CutMix (04b_train_cnn_cutmix.py) — el "intercambio de píxeles"
     mencionado explícitamente en el resumen de esta propuesta —, que
     recorta una región rectangular de una imagen del batch de
     entrenamiento y la pega sobre otra, mezclando las etiquetas en
     proporción al área intercambiada; se contrastó frente al
     augmentation geométrico estándar bajo hiperparámetros idénticos
     para comprobar si mitigaba el colapso de ese modelo, con
     resultado también negativo (ver 9.4 y 4.7).

  2. Diseñar, entrenar y comparar tres arquitecturas: CNN base, CNN
     con augmentation, y transfer learning (EfficientNetB0).

     CUMPLIDO. Se diseñaron y entrenaron las tres arquitecturas (ver
     detalle completo en la sección 4.1-4.3), pasando por tres rondas
     sucesivas de ajuste de hiperparámetros documentadas como parte
     del propio proceso científico: una primera ronda que reveló un
     colapso de entrenamiento en las dos CNN entrenadas desde cero
     (sección 5.1 / Hallazgo 1 del README), una segunda ronda que
     diagnosticó y corrigió la causa raíz (sustituir
     BatchNormalization por LayerNormalization, bajar el learning
     rate, aumentar la paciencia del `EarlyStopping`), y una tercera
     ronda que amplió el presupuesto de épocas disponible para
     comprobar si algún modelo seguía limitado por el techo anterior
     (Fase 8). El resultado final, con los tres modelos entrenados
     bajo el mismo protocolo y evaluados sobre el mismo conjunto de
     prueba, se resume en la tabla de la sección 9.4.

  3. Evaluar con sensibilidad, especificidad, exactitud, AUC-ROC y
     matriz de confusión; contrastar con una línea base de ML clásico
     (SVM + HOG).

     CUMPLIDO. Los cuatro modelos (los tres de Keras más la línea base
     SVM) se evaluaron sobre el mismo conjunto de prueba (165 imágenes
     nunca vistas durante el entrenamiento) con exactitud, AUC-ROC
     macro-average (one-vs-rest), F1 macro, sensibilidad Y
     especificidad por clase (uno-contra-el-resto, añadida en la
     Fase 10 para cubrir literalmente lo que pide este objetivo) y
     matriz de confusión completa, además de un informe de
     clasificación (precision/recall/f1/support) por modelo. La
     especificidad resultó ser la métrica que más claramente expone el
     colapso de CNN + Augmentation: 0.00 en Maligno (nunca descarta a
     nadie de esa clase) frente a un engañoso 1.00 en Benigno/Normal
     (trivial, por no predecirlas jamás) — ver sección 9.4 y la figura
     sensibilidad_especificidad.png. El contraste con la línea base
     SVM+HOG resultó, en sí mismo, uno de los hallazgos metodológicos
     más importantes del trabajo: su resultado perfecto (100% en las
     cinco métricas principales, incluida ahora la especificidad) no se
     interpretó como un éxito, sino como la evidencia empírica más
     clara de la limitación de fuga de datos del dataset (sección
     9.6 y Limitaciones) — un ejemplo concreto de por qué la
     evaluación crítica de resultados "demasiado buenos" es parte
     imprescindible de este objetivo, no un efecto secundario.

  4. Generar Grad-CAM sobre el mejor modelo y analizar su
     aplicabilidad en un flujo de trabajo de radiología.

     CUMPLIDO, con un resultado que matiza la aplicabilidad práctica.
     Se generaron mapas de activación Grad-CAM sobre el modelo de
     Transfer Learning (el más adecuado para explicabilidad por su
     mejor equilibrio de sensibilidad entre clases) para nueve
     imágenes de ejemplo (tres por clase). El análisis de esos mapas
     reveló un problema real: el mapa de calor está dominado por un
     único píxel saturado en una esquina fija de la rejilla de
     activaciones, idéntico para cualquier imagen de entrada,
     independientemente de la clase predicha (sección 5.3). Tras
     descartar que fuera un error de implementación —se encontró y
     corrigió un bug real de Keras 3 en el proceso— y verificar que el
     patrón persiste en tres entrenamientos distintos del mismo
     modelo, la conclusión es que Grad-CAM "clásico" tiene una
     aplicabilidad limitada sobre esta arquitectura concreta en su
     estado actual: no puede usarse, tal como está, como una
     explicación fiable de en qué se fija el modelo para clasificar
     una imagen. Esto cumple el objetivo de "analizar la
     aplicabilidad" precisamente porque la respuesta es un análisis
     crítico y no una validación automática — y abre una línea de
     trabajo futuro concreta (probar Grad-CAM++ o variantes menos
     sensibles a canales de activación atípicos).

--------------------------------------------------------------------
9.3 Desarrollo / Metodología (resumen narrativo)
--------------------------------------------------------------------
El desarrollo se organizó en cinco etapas secuenciales, implementadas
como un pipeline de scripts de Python numerados (01 a 08, en
src/), cada uno dependiente de los artefactos generados por el
anterior:

Primero, un análisis exploratorio de datos (01_eda.py) caracterizó el
dataset original: 1097 imágenes de TC de tórax repartidas en tres
carpetas por clase (Bengin cases, Malignant cases, Normal cases —los
nombres de carpeta vienen así del dataset original de Kaggle, con la
errata "Bengin" incluida), con tamaños originales heterogéneos y una
distribución de intensidad de píxel bimodal, coherente con imágenes
de TC en escala de grises donde coexisten tejido blando de baja
densidad y estructuras óseas/densas de alta densidad.

Segundo, el preprocesamiento (02_preprocessing.py) normalizó todas
las imágenes a una resolución común de 224×224 píxeles —la resolución
estándar de entrada para arquitecturas preentrenadas en ImageNet como
EfficientNetB0, lo que permite reutilizar exactamente el mismo pipeline
de datos para los cuatro modelos—, las convirtió a RGB, las escaló al
rango [0, 1] y generó una partición estratificada 70/15/15 con semilla
fija, guardada en un único archivo comprimido
(outputs/splits/dataset_splits.npz) para evitar reprocesar las
imágenes en cada entrenamiento.

Tercero, el entrenamiento de los tres modelos basados en CNN
(03_train_cnn_base.py, 04_train_cnn_aug.py, 05_train_transfer.py).
La CNN Base es una arquitectura sencilla de tres bloques
convolucionales (32→64→128 filtros) con normalización por capa
(LayerNormalization), max-pooling, *global average pooling* y una
cabeza densa final. La CNN + Augmentation reutiliza exactamente la
misma arquitectura, añadiendo una capa de aumento de datos en tiempo
real (flip horizontal, rotación ±0.08 radianes, zoom ±0.05,
variación de contraste) para observar el efecto aislado del
augmentation frente a la misma red sin él. El modelo de Transfer
Learning parte de EfficientNetB0 preentrenada en ImageNet, entrenando
primero solo una cabeza personalizada sobre la base congelada (fase 1)
y después descongelando las últimas 50 capas de la red preentrenada
para un ajuste fino de baja tasa de aprendizaje (fase 2). Los tres
modelos comparten el mismo protocolo de entrenamiento: optimizador
Adam, `class_weight="balanced"`, `EarlyStopping` sobre la pérdida de
validación con restauración de los mejores pesos, y
`ReduceLROnPlateau`; el número de épocas es un presupuesto máximo, no
un valor fijo, ya que el propio `EarlyStopping` determina cuándo
detener cada entrenamiento (ver el análisis detallado de por qué cada
modelo se detuvo en un número distinto de épocas en la sección 9.4).

Cuarto, la evaluación comparativa (06_evaluate.py) midió los tres
modelos de Keras sobre el conjunto de prueba —nunca visto durante el
entrenamiento— con un conjunto común de métricas (exactitud, AUC-ROC
macro-average, F1 macro, sensibilidad por clase, matriz de confusión),
y generó además una línea base de aprendizaje automático clásico
(08_baseline_ml.py): extracción de características HOG (*Histogram of
Oriented Gradients*) seguida de un SVM con kernel RBF, evaluada con el
mismo protocolo para poder compararla directamente con las CNN.

Quinto, la explicabilidad (07_gradcam.py) generó mapas de activación
Grad-CAM sobre el mejor modelo disponible, superponiendo sobre la
imagen original las regiones que más influyeron en la predicción,
como primer paso hacia un sistema cuyas decisiones puedan ser
auditadas por un radiólogo antes de confiar en ellas.

Todo el desarrollo se realizó sobre un flujo reproducible: cada script
puede ejecutarse de forma independiente desde la terminal, con una
semilla fija (SEED=42) en todos los puntos donde interviene
aleatoriedad, y los resultados (modelos, métricas, figuras) se
persisten en disco para que la evaluación y las figuras no dependan de
mantener una sesión de entrenamiento abierta. Además del pipeline de
terminal, se construyó una aplicación web (FastAPI + frontend HTML/
CSS/JS propio) que permite lanzar y monitorear el entrenamiento de
cualquiera de los cuatro modelos en vivo, clasificar imágenes nuevas
con explicabilidad Grad-CAM, y consultar la tabla comparativa de
resultados sin depender de la terminal — pensada como una
demostración práctica del sistema, no como el objeto de evaluación
científica del TFM en sí.

--------------------------------------------------------------------
9.4 Resultados
--------------------------------------------------------------------
La tabla siguiente resume los resultados FINALES de los seis modelos
sobre el conjunto de prueba (165 imágenes: 18 Benigno, 84 Maligno, 63
Normal — el modelo segmentado se evalúa sobre la versión segmentada de
ese mismo conjunto, misma composición y etiquetas):

Modelo                    Exactitud  AUC-ROC  F1 Macro  Sens.Benigno  Sens.Maligno  Sens.Normal
CNN Base                    0.8667    0.9449    0.7490      0.4444        0.9881       0.8254
CNN + Augmentation           0.5091    0.7233    0.2249      0.0000        1.0000       0.0000
Transfer Learning             0.7939    0.9563    0.7330      0.8333        0.8571       0.6984
Transfer + Segmentación         0.7879    0.9118    0.6968      0.5556        0.8929       0.7143
CNN + CutMix                      0.5091    0.7175    0.2249      0.0000        1.0000       0.0000
SVM (HOG) — línea base               1.0000    1.0000    1.0000      1.0000        1.0000       1.0000

Especificidad por clase (uno-contra-el-resto, añadida en la Fase 10
para cubrir el objetivo específico 3 de la propuesta del TFM):

Modelo                    Espec.Benigno  Espec.Maligno  Espec.Normal  Espec.Macro
CNN Base                     0.9184         1.0000         0.9020        0.9401
CNN + Augmentation             1.0000         0.0000         1.0000        0.6667
Transfer Learning                0.8299         1.0000         0.9118        0.9139
Transfer + Segmentación            0.8571         0.9630         0.8922        0.9041
CNN + CutMix                          1.0000         0.0000         1.0000        0.6667
SVM (HOG) — línea base                    1.0000         1.0000         1.0000        1.0000

(Fuente: outputs/figures/metricas_comparativas.json y
metricas_svm_baseline.json, generados por 06_evaluate.py y
08_baseline_ml.py respectivamente.)

Sobre el número de épocas: cada modelo se detuvo en un punto distinto
porque el presupuesto configurado (120 épocas para las dos CNN
entrenadas desde cero, 25+100 para las dos fases de transfer learning)
es un TECHO máximo, no una meta — `EarlyStopping` con paciencia 12
corta el entrenamiento en cuanto la pérdida de validación deja de
mejorar durante 12 épocas seguidas, restaurando los pesos de la mejor
época. En la práctica: CNN Base se detuvo en la época 34 (mejor punto:
época 22, con una exactitud de validación de 0.861); CNN + Augmentation
se detuvo en apenas la época 13, con su mejor punto siendo la propia
época 1 —nunca volvió a mejorar—, lo que por sí solo es evidencia de
que su problema no es de presupuesto de épocas sino estructural;
Transfer Learning completó 9 épocas en la fase de cabeza congelada y
52 de las 100 disponibles en la fase de ajuste fino. El SVM no tiene
concepto de épocas: converge con una única llamada a `.fit()` sobre
características ya extraídas.

Lectura de los resultados por objetivo:

  - Frente al objetivo de "diseñar, entrenar y comparar tres
    arquitecturas", el resultado más relevante es que NINGUNA de las
    tres domina en todas las métricas. La CNN Base, una vez corregido
    su colapso inicial, alcanza la exactitud y el F1 macro más altos
    de los tres modelos de Keras, pero mantiene una sensibilidad muy
    baja en la clase minoritaria Benigno (0.44 — deja sin detectar
    más de la mitad de los casos benignos). Transfer Learning tiene
    menor exactitud global, pero el AUC-ROC más alto de los tres y,
    sobre todo, una sensibilidad mucho más equilibrada entre las tres
    clases (0.83 / 0.86 / 0.70), lo que en un contexto de cribado
    médico —donde el coste de no detectar un caso Benigno o Maligno
    es alto— pesa más que un punto adicional de exactitud global. Por
    eso Transfer Learning se mantiene como el modelo recomendado del
    proyecto pese a no ganar en exactitud bruta. La CNN + Augmentation
    es el único de los tres modelos de Keras que no logró superar su
    colapso de entrenamiento: predice "Maligno" para prácticamente
    todo el conjunto de prueba (sensibilidad 1.00 en Maligno, 0.00 en
    las otras dos clases), un resultado que se mantuvo idéntico incluso
    tras triplicar el presupuesto de épocas disponible, reforzando que
    la causa es el propio aumento de datos en tiempo real interactuando
    con un dataset pequeño (24 *batches* por época), no una limitación
    de tiempo de entrenamiento.

  - Frente al objetivo de "contrastar con una línea base de ML
    clásico", el resultado —100% de exactitud, AUC-ROC y F1 en las
    tres clases— es la pieza de evidencia más contundente de todo el
    TFM sobre la limitación de fuga de datos del dataset: ningún
    clasificador clásico sin aprendizaje de representaciones debería
    alcanzar un resultado perfecto en un problema médico real de tres
    clases, así que ese 100% se interpreta como una señal de alarma
    metodológica, no como una victoria del método clásico sobre las
    CNN (ver sección 9.6 y Limitaciones para el razonamiento completo).

  - Frente al objetivo de generar y analizar Grad-CAM, el resultado
    (detallado en 9.2, punto 4, y ampliado en la sección 9.5 al
    explicar la figura) es que la explicabilidad actual del mejor
    modelo es limitada — un hallazgo honesto sobre las fronteras
    técnicas del propio sistema, no un fallo del proceso de
    investigación.

  - Frente al objetivo de implementar segmentación (ver 9.2, punto 1),
    el resultado del reentrenamiento sobre datos segmentados (sección
    4.6) es negativo: exactitud prácticamente igual (0.7879 vs 0.7939)
    pero AUC-ROC, F1 macro y sensibilidad Benigno más bajos que el
    modelo sin segmentar. El experimento de Grad-CAM asociado
    (¿desaparece el artefacto de la esquina al quitar el fondo?)
    confirma que SÍ desaparece esa forma concreta del artefacto, pero
    es sustituida por un patrón igual de constante (r=0.999 entre
    imágenes distintas) bajo una forma diferente — la segmentación
    clásica no resolvió el problema de fondo, solo lo desplazó. Ambos
    resultados se documentan tal cual, como evidencia metodológica
    válida sobre los límites de un enfoque de segmentación clásico
    (no aprendido) en este problema.

  - Frente a la mención explícita del resumen de la propuesta sobre
    "técnicas avanzadas de aumento de datos, como el intercambio de
    píxeles" (ver 9.2, punto 1, y sección 4.7), CutMix se implementó
    con los mismos hiperparámetros que CNN + Augmentation para una
    comparación limpia, y el resultado es que **reproduce el colapso
    número por número**: exactitud 0.5091 (idéntica), F1 macro 0.2249
    (idéntico), especificidad 0.00 en Maligno (idéntica). El
    entrenamiento muestra además que la pérdida de validación nunca
    mejora respecto a la primera época, pese a un pico transitorio de
    73.3% de exactitud de validación que `EarlyStopping` descarta por
    vigilar `val_loss` y no `val_accuracy`. Que dos técnicas de aumento
    de datos conceptualmente muy distintas —transformación geométrica
    por imagen vs. mezcla de píxeles y etiquetas entre pares de
    imágenes— converjan al mismo punto muerto es evidencia de que el
    colapso no depende del tipo de aumento, sino de la interacción
    entre el tamaño reducido del dataset, el desbalance de clases y el
    criterio de selección del mejor modelo. Es un resultado negativo,
    documentado con el mismo rigor que los positivos.

--------------------------------------------------------------------
9.5 Explicación de las figuras generadas (para incluir como pies de figura)
--------------------------------------------------------------------
Todas las figuras están en outputs/figures/ (excepto el grid de
Grad-CAM, en outputs/gradcam/). Se listan agrupadas por lo que
documentan.

ANÁLISIS EXPLORATORIO (generadas por 01_eda.py)

  - distribucion_clases.png: gráfico de barras con el número de
    imágenes por clase (Benigno 120, Maligno 561, Normal 416).
    Evidencia visual directa del desbalance de clases del dataset —
    Maligno tiene más de 4.5 veces las imágenes de Benigno—, que
    justifica metodológicamente el uso de `class_weight="balanced"`
    en el entrenamiento de los tres modelos de Keras.

  - distribucion_tamanos.png: dos histogramas (ancho y alto en
    píxeles) de las imágenes originales antes de normalizar. Muestra
    que, pese a que el rango declarado de tamaños es heterogéneo
    (506–801 px de ancho, 331–512 px de alto), en la práctica más del
    95% de las imágenes comparte un único tamaño (≈506×512 px), con
    solo un puñado de valores atípicos — es decir, la variabilidad de
    tamaño del dataset es mucho menor de lo que sugeriría el rango
    mínimo-máximo por sí solo. Justifica que redimensionar todo a
    224×224 px con interpolación LANCZOS sea una normalización segura
    y no introduzca una distorsión desigual entre clases.

  - distribucion_intensidad.png: histograma de densidad de la
    intensidad de píxel (0-255), superpuesto por clase. Muestra una
    distribución bimodal —un pico dominante en intensidades bajas
    (≈40-50, correspondiente al fondo y tejido blando) y un pico
    secundario más disperso en intensidades altas (≈190-230,
    estructuras densas/óseas)— con las tres clases casi totalmente
    superpuestas entre sí. La lectura metodológica relevante es que
    la intensidad de píxel por sí sola NO separa visualmente las
    clases, lo que justifica que la tarea requiera un modelo capaz de
    aprender patrones espaciales/texturales (como una CNN) en lugar de
    depender de estadísticos simples de intensidad.

  - muestras_por_clase.png: rejilla de 3×3 con tres cortes axiales de
    TC de tórax representativos por clase (Benigno, Maligno, Normal).
    Sirve como referencia visual cualitativa de cómo se ven las tres
    categorías: en los ejemplos de Maligno se aprecian masas/nódulos
    de mayor tamaño o densidad claramente visibles en el parénquima
    pulmonar, mientras que los ejemplos Normal muestran parénquima
    pulmonar sin hallazgos evidentes.

  - muestras_augmentation.png: una imagen original y nueve variantes
    generadas por la capa de aumento de datos (rotación, zoom,
    contraste, flip horizontal) usada en la CNN + Augmentation.
    Ilustra que las transformaciones aplicadas son sutiles y
    anatómicamente plausibles para una TC de tórax (por eso se
    descartaron explícitamente el flip vertical y la traslación, que
    no lo son) — relevante para la discusión de por qué el colapso de
    este modelo se atribuye a la interacción con un dataset pequeño y
    no a transformaciones agresivas o irreales.

  - ejemplos_segmentacion.png (Fase 10, generada por
    02b_segmentation.py): original, máscara binaria y resultado
    segmentado, un ejemplo por clase. Evidencia visual del algoritmo
    de segmentación (Otsu + restricción a la silueta corporal + top-2
    componentes conexos) tras la validación sobre 24+150 imágenes — ver
    Fase 10/Tarea 2 en la sección 3 para el bug real encontrado
    (la mesa del escáner colándose como si fuera un pulmón) y cómo se
    corrigió antes de procesar el dataset completo.

  - muestras_cutmix.png (Fase 10, generada por
    04b_train_cnn_cutmix.py): 8 ejemplos de un batch de entrenamiento
    tras aplicar CutMix, con la etiqueta mezclada resultante de cada
    uno. El parche rectangular intercambiado es visible a simple vista
    y la etiqueta mezclada corresponde exactamente a la proporción de
    área intercambiada (p. ej. [0.81/0.19/0.00] cuando el parche pegado
    cubre ≈19% del área) — confirma que la mezcla de píxeles y de
    etiquetas está implementada correctamente, antes de interpretar
    cualquier resultado de entrenamiento.

CURVAS DE ENTRENAMIENTO (una por modelo de Keras, generadas por
03/04/04b/05_train_*.py al final de cada entrenamiento)

  - curvas_cnn_base.png, curvas_cnn_augmented.png,
    curvas_transfer_learning.png: cada una muestra dos paneles
    (pérdida y exactitud) con las curvas de entrenamiento y
    validación por época. Son la evidencia visual directa de
    convergencia (o falta de ella): la de CNN Base muestra una
    convergencia sana, con la pérdida de validación bajando de forma
    sostenida hasta estabilizarse en la época 22 (val_loss 0.5875,
    val_accuracy 0.861); la de CNN + Augmentation muestra el patrón de
    colapso (la exactitud de validación se congela casi desde la
    época 1 —su propio mejor punto, val_loss 1.019, val_accuracy
    0.509— mientras la de entrenamiento sigue moviéndose); la de
    Transfer Learning muestra las dos fases del entrenamiento
    claramente diferenciadas (el salto de exactitud al iniciar la
    fase 2 de ajuste fino tras la época 9, hasta estabilizarse en
    val_loss 0.4993, val_accuracy 0.830).

  - curvas_cnn_cutmix.png (Fase 10): mismo patrón de colapso que
    curvas_cnn_augmented.png — el entrenamiento se detiene en la época
    13 (paciencia 12) porque el val_loss nunca mejora respecto a su
    valor en la época 1 (1.0476); la exactitud de validación llega a
    un pico de 73.3% en la época 9, pero ese pico queda fuera de los
    pesos restaurados porque `EarlyStopping` vigila `val_loss`, no
    `val_accuracy`, y esa época no tiene la menor pérdida.

COMPARACIÓN ENTRE MODELOS (generadas por 06_evaluate.py)

  - confusion_matrix_<modelo>.png (una por cada uno de los 6 modelos):
    matriz de confusión 3×3 (Benigno/Maligno/Normal) con el recuento
    exacto de aciertos y errores por par clase real/clase predicha.
    Es la evidencia más granular de CÓMO se equivoca cada modelo: la
    de CNN Base muestra que sus errores se concentran en confundir
    Benigno con Normal (10 de 18 casos Benigno se predicen como
    Normal); las de CNN + Augmentation y CNN + CutMix muestran, ambas,
    la columna "Maligno" completamente llena (0/18, 84/84, 0/63 en la
    fila correspondiente) — la firma visual inequívoca de un modelo
    colapsado a una sola clase, idéntica en ambos casos pese a ser dos
    técnicas de aumento distintas; la de Transfer Learning muestra
    errores más repartidos y en ambas direcciones; la del SVM muestra
    una diagonal perfecta sin ni un solo error, la evidencia visual más
    directa de la sospecha de fuga de datos.

  - classification_report_<modelo>.png (una por modelo): tabla en
    forma de imagen con precision, recall, f1-score y support por
    clase. Es una vista complementaria a la matriz de confusión, útil
    para citar cifras exactas de precisión y exhaustividad por clase
    en el cuerpo del texto sin tener que calcularlas de nuevo. La
    precisión por clase confirma el mismo patrón que ya mostraba la
    matriz de confusión: en CNN Base, precisión 0.400 en Benigno frente
    a 1.000 en Maligno y 0.839 en Normal (de cada 10 predicciones
    "Benigno", solo 4 son correctas); en CNN + Augmentation y en
    CNN + CutMix, precisión y recall en 0.000 para Benigno y Normal
    (nunca se predicen) y precisión ≈0.509-0.510 / recall 1.000 en
    Maligno en ambos casos; en Transfer Learning, precisión más pareja
    entre clases (0.375 / 1.000 / 0.830) que en CNN Base, reflejo de
    que su mejora no está en acertar más en total sino en repartir
    mejor los aciertos; en la SVM, 1.000 en las tres métricas para las
    tres clases.

  - curvas_roc_comparativas.png: las curvas ROC macro-average
    (one-vs-rest) de los 5 modelos de Keras + la SVM superpuestas en un
    único gráfico, con el AUC de cada una en la leyenda y la diagonal
    de un clasificador aleatorio como referencia. Permite comparar
    visualmente de un vistazo la capacidad discriminativa de todos los
    modelos: la curva de la SVM pegada a la esquina superior izquierda
    (AUC≈1.0) es, de nuevo, la representación visual más clara de que
    ese resultado no es creíble como generalización real; las curvas
    de CNN Base y Transfer Learning se superponen bastante entre sí,
    ambas claramente por encima de las de CNN + Augmentation y
    CNN + CutMix, que a su vez se superponen casi por completo entre sí
    (AUC 0.7233 vs. 0.7175) y se aproximan mucho más a la diagonal
    aleatoria en la zona de FPR bajo-medio.

  - tabla_comparativa.png: exactitud/AUC-ROC/F1/sensibilidad de los 5
    modelos de Keras (mitad de la tabla de la sección 9.4), renderizada
    como imagen para incluir directamente en el documento sin tener que
    recrear la tabla.

  - tabla_comparativa_especificidad.png (Fase 10): igual que la
    anterior pero con especificidad por clase en vez de sensibilidad,
    de los 5 modelos de Keras (paleta naranja, para distinguirla a
    simple vista de la de sensibilidad). Se separó en una figura propia
    en vez de añadir columnas a tabla_comparativa.png porque 10
    columnas en una sola tabla-imagen dejaban de leerse bien.

  - sensibilidad_especificidad.png (Fase 10): gráfico de barras
    agrupadas, 3 paneles (uno por clase: Benigno/Maligno/Normal), con
    sensibilidad y especificidad lado a lado para los 6 modelos (5 de
    Keras + SVM). Es la figura más reveladora del colapso de
    CNN + Augmentation y CNN + CutMix: en el panel Benigno sus barras
    de sensibilidad no existen (0.00); en el panel Maligno son sus
    barras de especificidad las que no existen (0.00), y ambos modelos
    son visualmente indistinguibles entre sí en los tres paneles pese a
    usar técnicas de aumento distintas — verlas juntas deja claro que
    ninguna de las dos métricas por separado cuenta la historia
    completa, y que un modelo puede lograr una "perfecta" en una clase
    por la razón trivial de no predecirla (o predecirla) nunca.

EXPLICABILIDAD (generada por 07_gradcam.py)

  - gradcam_grid.png: rejilla de 3×3 con nueve pares de imágenes
    (original + mapa de calor Grad-CAM superpuesto) del modelo de
    Transfer Learning, tres ejemplos por clase, con la predicción y la
    confianza de cada uno. Es la evidencia visual del hallazgo de la
    sección 5.3: en las nueve imágenes, el punto caliente del mapa de
    calor aparece siempre en la misma esquina inferior derecha,
    independientemente del contenido real de la imagen o de la clase
    predicha — la prueba visual directa de que el mapa no está
    señalando regiones anatómicas relevantes para la decisión del
    modelo.

  - gradcam_grid_segmentado.png (Fase 10, generada por
    07b_gradcam_segmentado.py): mismo formato, sobre el modelo
    Transfer Learning + Segmentación. El punto saturado en la esquina
    desaparece, pero se reemplaza por un degradado izquierda-derecha
    que ignora la forma de la máscara pulmonar (se extiende también
    sobre el fondo negro). Verificado que no es casualidad: la
    correlación de Pearson entre los mapas de calor crudos (7×7) de 3
    imágenes de contenido distinto es 0.999 — el mapa es, en la
    práctica, constante. La segmentación cambió la forma del problema
    de explicabilidad sin resolverlo.

--------------------------------------------------------------------
9.6 Discusión
--------------------------------------------------------------------
Tomados en conjunto, los resultados de este TFM sostienen tres ideas
centrales. La primera es que el diagnóstico y la corrección
sistemática de un problema de entrenamiento —el colapso observado en
la primera ronda— fue más informativo que si los modelos hubieran
convergido bien desde el principio: permitió aislar con evidencia
empírica que la causa era la combinación de `BatchNormalization` con
pocos *batches* por época y un *learning rate* alto, y demostrar que
corregirla arreglaba por completo un modelo (CNN Base) sin arreglar
del todo el otro (CNN + Augmentation), acotando así el augmentation
en tiempo real como un factor causal adicional de inestabilidad en
datasets pequeños.

La segunda es que "el mejor modelo" no es una pregunta con una única
respuesta correcta cuando las métricas no coinciden entre sí: la
tercera ronda de entrenamiento dejó a la CNN Base por delante en
exactitud y F1 macro, pero a Transfer Learning por delante en AUC-ROC
y en el reparto de sensibilidad entre clases. En un problema médico
real, donde el coste de un falso negativo en la clase minoritaria
(Benigno) no es equivalente al coste de un punto de exactitud global,
justificar la elección del modelo recomendado requiere hacer explícito
ese criterio de decisión —y no limitarse a reportar la cifra más alta
de una sola métrica.

La tercera es que la fiabilidad de una evaluación depende tanto del
modelo como del dataset y del propio diseño experimental. El 100% de
exactitud de la SVM+HOG es, paradójicamente, uno de los resultados más
valiosos del trabajo precisamente porque NO se interpretó como un
éxito: al no existir identificador de paciente en el dataset exportado,
es muy probable que cortes de TC casi idénticos del mismo paciente
queden repartidos entre entrenamiento y prueba, permitiendo que un
método sin capacidad real de generalización memorice apariencias en
lugar de aprender patrones patológicos. Esa misma limitación de
fuga de datos probablemente infla también, en algún grado no
cuantificable con los datos disponibles, las métricas de los tres
modelos de Keras — razón por la cual todas las cifras de este TFM
deben presentarse junto con esa advertencia, y no como una medida
absoluta de la capacidad diagnóstica del sistema. En la misma línea,
la caída de exactitud de Transfer Learning en la tercera ronda pese a
mejores métricas de validación es evidencia práctica —no solo teórica—
de que un único split fijo de validación/prueba de 165 imágenes cada
uno introduce más varianza de la que aparenta una cifra con cuatro
decimales, lo que motiva validación cruzada como mejora metodológica
prioritaria de cara a un trabajo futuro.

La cuarta es que una intervención de preprocesamiento intuitivamente
razonable —segmentar los pulmones para que el modelo solo vea tejido
relevante— no garantiza una mejora, y verificarlo empíricamente en vez
de asumirlo es en sí mismo parte del método científico. La segmentación
clásica implementada no mejoró ninguna métrica agregada del modelo
recomendado, y empeoró notablemente su sensibilidad en la clase
minoritaria (Benigno). Más revelador aún: el problema de explicabilidad
de Grad-CAM sobrevivió a la segmentación, solo cambiando de forma (de
un punto saturado a un degradado), y una verificación cuantitativa
(correlación 0.999 entre mapas de calor de imágenes distintas) mostró
que en realidad es igual de severo que antes. Esto apunta a que la
causa del problema de explicabilidad está más en cómo EfficientNetB0
integra información espacial en su última capa que en qué contenga la
imagen de entrada — una hipótesis que ninguna intervención de
preprocesamiento, por sí sola, puede resolver.

La quinta es que un colapso de entrenamiento reproducido de forma
idéntica bajo dos técnicas de aumento de datos conceptualmente muy
distintas es evidencia más fuerte que un solo experimento sobre dónde
está la causa real del problema. CutMix (mezcla continua de píxeles y
etiquetas entre pares de imágenes) y el augmentation geométrico
estándar (transformación de cada imagen por separado) no comparten
ningún mecanismo en común más allá de "introducir variabilidad
adicional en el batch de entrenamiento" — y sin embargo ambos producen
exactamente el mismo resultado (0.5091 de exactitud, el mismo patrón de
matriz de confusión, la misma especificidad 0.00 en Maligno), con la
pérdida de validación estancada desde la primera época en ambos casos.
Esa convergencia hacia el mismo punto muerto, pese a partir de técnicas
distintas, señala que la causa no es una propiedad de ninguna técnica
de aumento en particular, sino de cómo cualquier fuente de variabilidad
adicional interactúa con un dataset pequeño (767 imágenes de
entrenamiento), un desbalance de clases notable, y un criterio de
`EarlyStopping` que selecciona el mejor modelo por pérdida de
validación — bajo esas condiciones, predecir siempre la clase
mayoritaria es, literalmente, la estrategia que minimiza esa pérdida
más rápido que aprender a discriminar de verdad.

--------------------------------------------------------------------
9.7 Conclusiones (borrador)
--------------------------------------------------------------------
Este trabajo desarrolló y evaluó un sistema de apoyo al diagnóstico
para la clasificación de imágenes de TC de tórax en tres categorías,
cumpliendo los cuatro objetivos específicos planteados: se preprocesó,
segmentó y caracterizó el dataset IQ-OTH/NCCD, se diseñaron, entrenaron
y compararon tres arquitecturas de CNN frente a una línea base
clásica, se evaluaron con un conjunto común de métricas estándar de
clasificación médica (incluida sensibilidad Y especificidad por
clase), y se generaron e interpretaron críticamente mapas de
explicabilidad Grad-CAM sobre el mejor modelo y su variante segmentada.

Los resultados muestran que, tras diagnosticar y corregir un problema
real de inestabilidad de entrenamiento, es posible entrenar modelos
competitivos incluso desde cero sobre un dataset reducido (CNN Base:
86.67% de exactitud), y que el aprendizaje por transferencia, aunque
no siempre gane en exactitud bruta, ofrece el mejor equilibrio de
sensibilidad entre clases (Transfer Learning: AUC-ROC 0.956,
sensibilidad entre 0.70 y 0.86 en las tres clases), lo que lo convierte
en el modelo recomendado para un caso de uso de cribado donde no
detectar un caso tiene un coste alto. Al mismo tiempo, el trabajo
documenta con la misma transparencia los resultados que no favorecen
al sistema: un modelo (CNN + Augmentation) cuyo colapso de
entrenamiento no se logró resolver por completo (con especificidad
0.00 en Maligno, la evidencia más clara de ese colapso), una segunda
técnica de aumento de datos avanzada (CutMix, el "intercambio de
píxeles" de la propuesta) que reproduce ese mismo colapso número por
número bajo hiperparámetros idénticos —evidencia de que la causa no es
el tipo de aumento sino su interacción con un dataset pequeño y
desbalanceado—, una línea base clásica cuyo resultado perfecto es
evidencia de fuga de datos y no de capacidad real, un experimento de
segmentación pulmonar clásica que no mejoró el modelo recomendado, y
una limitación de explicabilidad en el modelo recomendado —que
persiste, solo que con otra forma, incluso tras segmentar— que impide,
en su estado actual, ofrecer al radiólogo una justificación visual
fiable de cada predicción.

La conclusión general es que el sistema desarrollado cumple su
objetivo como PROTOTIPO ACADÉMICO de apoyo al diagnóstico, pero no
está listo —ni pretende estarlo— para un uso clínico real: la fuga de
datos por ausencia de identificador de paciente es una limitación
metodológica de fondo que exigiría, como mínimo, reconstruir el split
a nivel de paciente y validar sobre un conjunto externo antes de
considerar cualquier aplicación fuera del ámbito académico. Dentro de
esas fronteras, el proyecto demuestra de forma honesta tanto el
potencial como los límites concretos de aplicar CNNs y transfer
learning a un problema de imagen médica con recursos de datos
limitados — que es, en última instancia, la situación realista a la
que se enfrenta gran parte de la investigación aplicada en este campo.
